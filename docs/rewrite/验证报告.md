# 验证报告（rewrite/v2）

结论：**92/92 测试通过，重写分支行为与基线一致（含 3 处经裁定的显式例外）。**

## 1. 测试总览

| 阶段 | 用例数 | 说明 |
|---|---|---|
| 重写前基线（master） | 65 | 阶段零盘点 |
| 重写后（rewrite/v2 2a466bf） | 92 | +27：图开关/purge×7、tracker 等价×6、缓存 LRU×6、无 GPU 契约×2、离线端到端×2、合成器契约×5（含真机 CUDA 用例） |

每步提交前全量跑 `python -m tests.run_tests`，0 失败后才 commit（提交信息带 `[N/N]` 标注）。

## 2. 行为一致性验证矩阵

| 对外行为 | 验证方式 | 结果 |
|---|---|---|
| 18 元 config ↔ synthesizer 构造/加载 | `test_models_synthesizer`：strict 权重往返 + 键结构断言 | ✅ |
| 真实模型离线转换端到端 | `test_streaming_offline`：真模型+golden wav，采样率/时长/FLOAT32/非静音/峰值 0.99 | ✅ |
| RMVPE 4 帧边缘偏移（未文档化行为） | `test_pipeline_tracker`：与旧切片实现逐元素相等 | ✅ |
| CUDA Graph 捕获/回放/清理 | `test_runtime_graph`：真机 CUDA 捕获→回放→purge→再捕获 | ✅ |
| 缓存 LRU 容量与淘汰语义 | `test_pipeline_cache`：容量 2/1/1/2、同 key 覆盖、无 mtime 失效 | ✅ |
| 无 GPU 报错契约 | `test_runtime_device`：RuntimeError 中文文案、无 CPU 回退 | ✅ |
| 训练 checkpoint 保存/淘汰/合并/导出格式 | 既有 `test_train_checkpoint`（含 WEIGHTS_DIR 修复路径） | ✅ |
| ffmpeg 异常契约 / WAV 读写 | 既有 `test_io_*` | ✅ |
| GUI offscreen 冒烟（窗口构建+绑定往返） | 既有 `test_gui_*`（覆盖删 hasattr 后的 bindings 路径） | ✅ |
| autodl 向导交互/退出码 0/1/2 | 金标准：子进程喂答案逐行 diff | ✅ |
| 架构依赖方向（八层无环、View 禁 import rvc） | AST 静态检查 `test_architecture` | ✅ |
| dsp 纯函数（SOLA/RMS/mel/Hz-MIDI） | 既有数值测试（A8 锁定未改动） | ✅ |

## 3. 手工验证项（自动测试无法覆盖，待用户真机确认）

- 实时变声主观音质（延迟读数、音域映射听感）——算法路径零改动，预期无差异
- 托盘行为、设备热插拔刷新（DeviceCatalog 仅改名）
- 训练 GUI 完整训练一轮（P0-1 修复后首次保存路径已由 checkpoint 测试覆盖，但整轮训练建议真机跑一次）

## 4. 已知残留（有意保留）

- `sounddevice` 私有 API 依赖：已封装单点（`_rescan_portaudio_devices`），依赖本身仍在（PortAudio 无公开重扫 API）
- `Config` 单例硬编码 `cuda:0` + `is_half=True`：现状契约，无 CPU 回退
- 离线/实时共享 Runner：按用户裁定保留原样，契约已写成显式注释
- `Retrieval-based-Voice-Conversion-WebUI/`：参考项目保留，零引用（架构测试排除该目录）

## 5. 提交清单（master..rewrite/v2，12 笔）

| 提交 | 步骤 | 测试 |
|---|---|---|
| `8a1ea9b` | 保全重写前未提交的 dsp/pipeline 性能改动 | — |
| `32ec6ca` | S1 基线快照 + 阶段 0-2 文档 | 65 |
| `da8b7b4` | S2 删死物 | 65 |
| `9c2ea42` | S3 P0-1 WEIGHTS_DIR → export_dir | 65 |
| `c4fad11` | S4 P0-4 无 GPU 报错 + P1-1 error_count 标签 | 66 |
| `6ea91b8` | S5 RuntimeOptions + purge 单入口 | 73 |
| `c655bef` | S6 io 清理 | 73 |
| `eff1976` | S7 pipeline 常量化 + sessions 公共 API | 85 |
| `bd9b32c` | S8 loudness→effects + 离线 e2e 重建 | 87 |
| `3cfa269` | S9 合成器参数化 + commons→vits_blocks | 92 |
| `8355ab9` | S10 data_utils→dataset + losses 锁定 | 92 |
| `2a466bf` | S11 DeviceCatalog + 删 hasattr 探测 | 92 |

（S12：README 更新与本对照表/验证报告为收尾提交。）

## 6. 过程风险记录

重写全程在本机已知的 git ref 撕裂环境下进行：每笔提交后手工核对并修补
`.git/packed-refs`（完整 40 位 SHA）；一次 `git rm` 触发 tests/ 目录撕裂，
已用 `git show HEAD:<path>` 全数恢复并经 `git ls-files` 磁盘完整性校验。
最终 HEAD=2a466bf，工作区干净，无 tracked 文件缺失。
