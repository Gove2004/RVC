# 现状问题清单（按严重度）

> 阶段一产出。P0 = 正确性/健壮性缺陷，重写必须解决；P1 = 结构性债务，重写核心动机；P2 = 风格/卫生，顺手清理。
> 每条附证据（文件:行号）。行为语义本身（如"异常类型不一致"）是否"修复"取决于其是否对外可观测——对外可观测的不一致**保持原样**，仅内部可见的按 P0 修复。

---

## P0 — 正确性与健壮性（重写中必须解决，且不改变对外行为）

| # | 问题 | 证据 | 处理方向 |
|---|---|---|---|
| P0-1 | **`InferenceContext.formant_factor` 未声明的动态字段**：`reset()` 不清它，跨块残留靠"每块必定重写"的隐式约定保正确 | pipeline/context.py:72-74、pipeline.py:154、runner.py:328 | 声明为正式字段并纳入 reset |
| P0-2 | **checkpoint/合成器权重 `strict=False` 加载零警告**：缺键、形状不匹配全部静默跳过，坏模型表现为"能跑但音质异常" | checkpoint.py:29-41、loader.py:59 | 内部加 warning（对 GUI 呈现保持现状：弹 str(e) 通道不变） |
| P0-3 | **audio_file 异常类型不一致**：同文件里 ffmpeg 缺失抛裸 FileNotFoundError、解码失败抛 AudioLoadError | io/audio_file.py:24 vs :46 | 对外类型保持两种；内部统一由一个明确的 ffmpeg 缺失异常派生 |
| P0-4 | **`_human_size` 负数返回 None**（隐式 return None），下游格式化会崩 | autodl/prompts.py:88-93 | 修（内部纯函数，不可观测） |
| P0-5 | **LoadThread 无法真中断**：3s 超时后带着运行中的线程断开信号继续，其后 emit 无人接收 | gui/infer/view/window.py:645-669 | 重写加载生命周期（对外呈现保持"加载中不可取消"或按现状文案） |
| P0-6 | **预热线程从非 Qt 线程调 `QTimer.singleShot`**：靠 0ms 单发碰巧可用，官方不允许 | gui/infer/view/window.py:268 | 信号转主线程 |
| P0-7 | **训练侧/推理侧 RMVPE 阈值双源不一致**（0.03 vs 0.05）且注释自称的 FCPE 0.025 与实际 0.05 也不符 | pipeline/pitch/postprocess.py:21-22 vs core/config.py:30 | **保持数值现状**（行为基线 4.3），仅消除双源：单一常量定义+两处显式取值 |
| P0-8 | **sounddevice 私有 API** `sd._terminate()/_initialize()`，上游升级即碎 | rvc/io/devices.py:12、gui/infer/controller/device_controller.py:21-23 | 无替代公开 API——保留但隔离到单点+注释原因（不引新依赖） |
| P0-9 | **训练产物目录被二次写入**：spec 缓存写进 `0_gt_wavs/`，污染切片产物目录且被指纹清目录误伤风险 | rvc/train/data_utils.py:78-98 | 缓存移到独立子目录（产物文件本身不变——目录是内部布局，0_gt_wavs 内容对用户"多出缓存文件"属可感知但低风险，已列入基线 4.11，改动需在验证报告标注） |
| P0-10 | **训练 GUI 停止时主线程直接 emit 信号**（request_stop 在主线程 emit stage_changed/log_message，与 worker 线程 emit 混用） | gui/train/controller/workers.py:39-40 | 统一经信号队列 |
| P0-11 | **pinned memory 竞态**：`_stage_input` non_blocking DMA 后下一块即覆写 pinned 缓冲，输入被污染 → HuBERT(fp16) 输出整块 NaN（阶段三 S0 bisect 实证，块序随运行变化） | streaming/runner.py:264-274 | 拷贝后同步（或改用同步拷贝） |
| P0-12 | **双 CUDA Stream 并发 fp16 推理未定义行为**：HuBERT 与 RMVPE 并发执行，cuBLAS/cuDNN 工作区跨流并发实测随机产生整块 NaN（S0 bisect 实证：NaN 仅出现在 HuBERT 输出、约 13% 块、块序随机）；实时变声的偶发音爆/杂音疑与此相关 | pipeline/pipeline.py:157-173 | 重写串行化阶段2/3（或逐模型 CUDA Graph 单流回放） |
| P0-13 | **同进程第二个引擎实例输出 NaN**：全局 CUDA Graph/静态缓存跨实例污染（S0 实证：同进程连建两个 RealtimeEngine，第二个输出 NaN；竞态补丁后仍谱差 6dB） | pipeline/cache.py、runtime/cuda_graph.py 全局态 | 引擎实例隔离或显式 lifecycle 约束 |
| P0-14 | **块级异步链路缺块尾同步**：D2H 输出与下一块 GPU 写入存在时序窗口（S0 实证：块尾 synchronize 后输出稳定有限） | streaming/runner.py:164-235 | 每块显式同步点 |

## P1 — 结构性债务（重写核心动机）

### 依赖方向（全部已验证）
| # | 问题 | 证据 |
|---|---|---|
| P1-1 | `models` → `pipeline`（模型定义层反向依赖推理编排层）：hubert 用推理缓存、rmvpe 用 cuda_graph | models/hubert.py:15、models/rmvpe/model.py:11 |
| P1-2 | `models` → `train`：rmvpe/transforms 借 train.mel 的滤波器函数 | models/rmvpe/transforms.py:7 |
| P1-3 | `pipeline` ↔ `streaming` 互指：pipeline.state 内嵌 streaming.AudioProcessor；streaming.runner 又 import pipeline.synthesis | pipeline/state.py:20 ↔ streaming/runner.py:38 |
| P1-4 | 模块级循环靠**函数内延迟 import** 规避：registry↔pitch.extractor、engine↔pipeline、wav_file↔audio_file（私有符号 `_ffmpeg` 跨模块） | registry.py:96、engine.py:164、wav_file.py:129 |
| P1-5 | gui View 越层：窗口穿透 engine→runner→pipeline→state 四层私有属性读音高（包 except 吞掉）；托盘摸 controller 私有属性；train 侧三个工具 Tab 直调 rvc.train（无 controller 层）；View 直接拼 PRETRAINED_ROOT/MODELS_DIR 路径 | window.py:487、tray.py:92/100-102、tools_merge.py:72 等、train/view/window.py:51-53、global_params_tab.py:10 |
| P1-6 | autodl 全局副作用：**导入即 chdir**；两处跨模块 monkey-patch（audio_file.FFMPEG_EXE、trainer.WEIGHTS_DIR）——模块全局可变状态当配置通道 | autodl/__init__.py:8、envcheck.py:150-157、steps.py:141、trainer.py:33 |

### 重复与不一致
| # | 问题 | 证据 |
|---|---|---|
| P1-7 | 实验参数默认值**三处重复**（dataclass / bindings / window 迁移表），且相互漂移：UI 初始勾 FCPE 但配置默认 rmvpe；epochs GUI 200 vs TrainConfig 2000；lr "1e-4" vs "0.0001" | core/config.py:18-31、bindings.py:74-97、audio_driver_tab.py:64、settings_tab.py:63、train_state.py:30/33 |
| P1-8 | SOLA 缓冲计算 `min(crossfade, 4*zc)` 双实现；prefetch_input 与 _stage_input 几乎逐行重复；`enable_out2` 派生逻辑两份；配置保存 try/except 两份；theme.py 两段 QSS 整段重复；三个工具 Tab 样板三份且互相 import 私有函数 | runner.py:84/loudness.py:58、runner.py:237-274、bindings.py:93/236、window.py:627-636/833-843、theme.py:105-127、tools_*.py |
| P1-9 | 三套"阶段编号"并存（pipeline"2-4"、runner"1-5"、context"1-8"），方法名 `_stage_5a` 挂在自称"阶段2-4"的类上 | pipeline.py:1/284/322、runner.py:7-12、context.py:29-57 |
| P1-10 | 魔法哨兵字符串 `"不使用"` 表示副输出关闭；`f0_rmvp_btn` 拼写错误（rmvpe）；`DeviceManager/OfflineManager` 与 `*Controller` 命名体系混用 | device_controller.py:77、audio_driver_tab.py:60、device_controller.py:5 |
| P1-11 | 2a_f0（下划线）vs 2b-f0nsf（连字符）目录命名不一致 | preprocess.py:16-23 |

### God class / 状态管理
| # | 问题 | 证据 |
|---|---|---|
| P1-12 | `MainWindow` 860 行：UI 构建+信号连接+配置读写迁移+预热线程+LoadThread 生命周期+托盘退出+离线委托+定时刷新；`TrainWindow` 同病 | gui/infer/view/window.py、gui/train/view/window.py |
| P1-13 | 信号槽 spaghetti：循环闭包连接+`_signals_connected` 布尔防重、旧线程逐个 disconnect 捕获 RuntimeError 继续、blockSignals 防递归、自定义信号 deleteLater 竞态靠注释规避 | window.py:414-459/645-669、device_controller.py:44-48、offline_controller.py:251-255 |
| P1-14 | 引擎门面 `RealtimeEngine` 同时管：设备全局状态（sd.default.device）、加载线程、错误计数、离线模拟、双流路由——职责过载 | streaming/engine.py 全文（且全文件双倍行距，格式异常） |

## P2 — 卫生问题（清理即可）

| # | 问题 | 证据 |
|---|---|---|
| P2-1 | 死代码（rvc）：rmvpe/transforms.py `STFT` 类整文件（docstring 乱码 GBK/UTF-8）、`RMVPE.resample_kernel`、`Config.gpu_mem`、`AudioStreamManager.check_health`、`manifest_matches`、`clip_grad_value_` 恒 no-op 调用、不可达 `n_gru=0` 分支（引用不存在的 `torch.nn.N_MELS`）、`sr2sr` 旧模型卡映射、`MultiHeadAttention.attn=None` 残留 | transforms.py 全文、model.py:18、device.py:30、stream.py:191、preprocess.py:191、trainer.py:173/187、rmvpe/blocks.py:246、synthesizer_model.py:15-19、attentions.py:94 |
| P2-2 | 死代码（gui）：`_sl`/`_sl_value_as_float`（后两者被 import 但从未调用）、styles 的 sep/CardStyles/MiscStyles/LabelStyles.bold、bindings 的 CHECK/TEXT 读写方式（表内无条目使用） | widgets.py:11-14/83-85、components.py:151-223、bindings.py:14/18 |
| P2-3 | 死引用：注释/docstring 引用不存在的 `rvc.inference.*` 模块（改名残留）；engine_controller docstring 引错配置模块路径 | extract_f0.py:15、rmvpe/constants.py:3、runtime/cuda_graph.py:3、engine_controller.py:3 |
| P2-4 | `__pycache__` 留有 6 个已改名模块的编译残留（device_manager/main_controller/main_window×2/train_state 等） | gui 各子目录 |
| P2-5 | 调试遗留：回调内 "[DEBUG] block=%d EXCEPTION" 日志+计数器；extractor.py 孤立悬挂注释 | engine.py:372-374、extractor.py:21-22 |
| P2-6 | pipeline.py import 位置混乱（`_is_cuda` 定义夹在 import 中间） | pipeline.py:26-33 |
| P2-7 | 命名违规候选（重写时按新规范命名）：`ModelSessionManager`（Manager）；全库 `data/info/helper` 级命名暂未发现，但 gui 侧 `state/bindings` 的 BINDINGS 表键为点号字符串路径（隐式耦合控件属性名） | registry.py:33、bindings.py:25-44 |
| P2-8 | run_tests.py 显式类清单需手工同步（新测试类漏登记不报错）；测试断言宽松（如 fast_rms 帧数只查区间） | tests/run_tests.py:22-33、test_rms_mix.py:25-30 |
| P2-9 | 日志时间戳无年份（autodl logger）；`_probe_packages` 类型注解与实际语义不符；`_scan_audio`/Windows 内存探测静默降级 | autodl/logger.py:52、envcheck.py:52-79/27-36 |
| P2-10 | save_state.json 含本机绝对路径与设备名（用户状态文件，非缺陷，但重写后字段结构变化需在 .bak 迁移策略上明确"不迁移旧字段"） | assets/configs/save_state.json |

---

## 严重度统计

- **P0：14 条**（1 条需基线同步：spec 缓存目录；2 条保持现状仅消除双源：RMVPE 阈值、异常类型二元性；P0-11~14 为阶段三 S0 金标准采集时实证的并发缺陷）
- **P1：14 条**（依赖方向 6、重复/不一致 5、结构 3）
- **P2：10 条**

## 与功能基线的对应关系

- P0/P1 的处理**全部不改变**《01-功能基线清单》所列对外行为；仅 3 处涉及"用户可感知的边缘变化"已在基线第 9 节列为已批准行为变化（models 字段删除、experimental 迁移删除、spec 缓存目录）。
- 测试重写策略：删旧 28 例，新测试以基线清单 `[已有测试]` 语义断言为验收标准，并按基线第 10 节补齐盲区（F0 边界、持久化、autodl 管道输入、checkpoint 互操作）。
