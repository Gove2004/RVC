# 现状问题清单（按严重度）

> 阶段一产物 · 2026-09-16。严重度：P0 崩溃/数据错误 · P1 行为隐患/维护陷阱 · P2 结构坏味道/清理项。
> 每条标注处置：重写时 修复（可保持对外行为）/ 保持（是现状对外行为，动它=违约）/ 清理。

---

## P0 — 崩溃与数据错误

| # | 位置 | 问题 | 处置 |
|---|---|---|---|
| P0-1 | `rvc/train/trainer.py:159,249,255` | 使用**未定义**的名字 `WEIGHTS_DIR`（仅导入 MODELS_DIR）。训练首次 `_save()` 即 NameError；`train()` 返回值同样崩。49 行 `export_dir` 参数本就是为替代它而加，重构断线遗留。当前训练功能实际不可用 | 修复：统一走 `export_dir`（默认 `assets/models/`），对外契约按基线 5.7 |
| P0-2 | `rvc/streaming/engine.py` | `process_file` 直接替换 `self._runner`/原地 update `runtime_params`，离线与实时共享同一 Runner——离线转换会污染实时引擎状态（反之亦然），实时运行中触发离线转换行为未定义 | **保持**（用户裁定 2026-09-16：实时运行中不做离线，就用同一 engine；约束写入文档与注释） |
| P0-3 | `rvc/io/audio_file.py` / `rvc/runtime/paths.py` | ffmpeg 二进制硬编码 Windows `.exe`，无 POSIX 回退——README 宣称支持 Linux，实际离线加载与训练预处理在 Linux 全挂 | 保持 Windows 硬编码；**Linux 兼容仅 autodl 需要**（用户裁定 2026-09-16），envcheck 现有三级定位（RVC_FFMPEG→PATH→assets）维持不动；README 如实化 |
| P0-4 | `rvc/runtime/device.py` | 硬编码 `cuda:0`+`is_half=True`：无 NVIDIA GPU 启动即崩，错误信息裸 `torch` 异常而非 RVCError，用户无从理解 | 错误显式化修复：崩但给中文可读错误（对外行为从"裸栈崩溃"变"可读报错"——属错误信息改善，需拍板） |

## P1 — 行为隐患

| # | 位置 | 问题 | 处置 |
|---|---|---|---|
| P1-1 | `rvc/streaming/engine.py` `_cb` | 回调异常分支：`should_stop=False` 时静默吞错、清零输出、继续运行。用户听到静音却不知道出错 | 保持吞异常行为；**GUI 增加展示 error_count**（用户已拍板：底部栏新增错误标签，仅 >0 时显示） |
| P1-2 | `rvc/pipeline/loader.py` | `torch.load(weights_only=False)` 反序列化任意代码，加载第三方 pth 即任意代码执行。模型文件来源可信时是 RVC 生态现状约束 | 保持（生态兼容优先），代码注释显式声明安全前提 |
| P1-3 | `rvc/pipeline/pitch/tracker.py` | 写 pitch 缓存用魔法偏移 `f0[3:-1]`/`write_start = 4 - len`，依赖 RMVPE mel padding 的 4 帧边缘行为——上游实现一变即静默错位 | 重写：改为显式命名常量 + 单元测试锁定偏移语义 |
| P1-4 | `rvc/pipeline/pitch/extractor.py` | `_FilteredStream` 全局猴补 sys.stdout/stderr 吞 torchfcpe print 噪声；`hubert.py` 同样在无控制台时全局换 devnull——模块级全局流副作用，相互干扰风险 | 重写：限定作用域的 context manager 或日志层过滤；对外保持"控制台无 FCPE 噪声" |
| P1-5 | `rvc/io/devices.py` | 使用 sounddevice 私有 API `sd._terminate()/_initialize()` 重建设备列表——sounddevice 升级即崩 | 保持行为，封装成独立函数并注释版本锁定前提 |
| P1-6 | `rvc/runtime/device.py` + `graph.py` | 跨模块靠环境变量 `RVC_CUDA_GRAPH=1` 通信（模块 import 顺序敏感）；CUDA Graph 清除必须 gc+sync+empty_cache 否则新旧图内存地址冲突（症状：声音沙哑/全零）——隐式时序契约，无任何断言保护 | 重写：显式配置对象传递；清除时序封装为单一入口 + 测试 |
| P1-7 | `rvc/pipeline/sessions.py` | `clear_all` 直接摸 `inference_cache._synthesizer` 私有属性；`get_f0_extractor` 疑似死方法 | 修复 + 清理 |
| P1-8 | `gui/configs/config.py` | 启动时合并旧 `"experimental"` 键——旧版兼容层，与"不兼容旧版本"原则冲突；但现存用户 save_state 可能含该键 | 保持读取合并（一次性迁移），重写后两个版本再删 |
| P1-9 | `tests/golden/offline_*.npy` | 离线端到端金标准被删后遗留死物——离线转换全链路目前**零测试覆盖**（对照基线 2.7） | 清理死物 + 阶段三重建离线端到端测试 |
| P1-10 | `rvc/pipeline/cache.py` | 全局单例 `default_inference_cache` 跨引擎共享状态，模型文件被外部替换（重训/覆盖）后缓存不失效，加载到旧权重且无提示 | 保持 LRU 语义（对外行为），测试锁定 |

## P2 — 结构坏味道 / 清理项

| # | 位置 | 问题 | 处置 |
|---|---|---|---|
| P2-1 | `Retrieval-based-Voice-Conversion-WebUI/`（259 文件） | 原版上游完整副本，代码零引用。**用户裁定：保留为参考项目**，排除在重写/清理之外 | 保留 |
| P2-2 | `assets/ffmpeg/ffprobe.exe`、`assets/rmvpe/rmvpe_inputs.pth` | 零引用死文件 | **保留**（用户裁定 2026-09-16，不作清理） |
| P2-3 | `tests/golden/offline_a.npy`、`offline_b.npy`、`manifest.json` | 死测试数据 | 清理（P1-9 重建后） |
| P2-4 | `rvc/core/config.py` | `TrainConfig.log_interval` 死字段（注释已自认） | 清理 |
| P2-5 | `gui/configs/config.py` | 默认骨架 `"models": []` 键从不写入 | 清理 |
| P2-6 | `rvc/streaming/loudness.py` | 名不符实：实为输出效果器编排（RMS+SOLA），"loudness" 误导；真正的"响度"只有 engine.measure_ms 延迟测量 | 重命名 |
| P2-7 | `rvc/io/wav_file.py` | docstring 引用不存在的 "wav_io.py/loader.py"（过时注释）；`mel_processing.py` 残留未用 `MAX_WAV_VALUE` | 清理 |
| P2-8 | `rvc/models/synthesizer_model.py` | `del self.enc_p` 重建 768 维：先构建 256 维再删，浪费构造且易漏改 | 重写为按维度参数化构造（权重键名不变） |
| P2-9 | `rvc/nn/commons.py` | slice_segments Python for 循环逐 batch 切片，可向量化——但为保数值一致，重写阶段不动，仅在阶段二评估 | 评估 |
| P2-10 | `gui/` 多处 | `hasattr(win, widget)` 防御式探测遍布 bindings/apply/collect——控件存在性本应静态可知，运行时探测掩盖页签结构改动 | 重写：绑定表与页签构建同源，删 hasattr 探测（行为不变） |
| P2-11 | `rvc/pipeline/pitch/postprocess.py` 等多处 | 双层缓存/魔数上限（128、2048 帧、5120 样本对齐）缺测试锁定 | 阶段三补测试 |
| P2-12 | 全项目 | 测试覆盖空洞：托盘、closeEvent、DeviceManager、实时音频流、TrainWorker 协作停止、autodl 实际训练路径（金标准只走到取消） | 阶段三按风险补：设备契约、状态持久化、协作停止；音频流保持手工验证矩阵 |

## 处置汇总（用户已裁定，2026-09-16）

1. **P0-1** 修复后对外行为不变（训练首次能保存），属"修 bug 非改行为"。
2. **P0-2** 保持现状：离线与实时共用同一 engine，实时运行中不做离线（用户裁定）。
3. **P0-3** ffmpeg 路径保持 Windows 硬编码；Linux 兼容仅 autodl envcheck（已有三级定位）。
4. **P0-4** 无 GPU 从裸栈崩溃改为中文可读报错（错误文案变化，已获认可）。
5. **P1-1** 保持吞异常行为，GUI 底部栏新增 error_count 展示。
6. **P2-2** ffprobe.exe、rmvpe_inputs.pth 保留。
7. **P1-8** 旧配置键迁移保留一个版本，重写 v2 再删。
