# S4 · 数值对照表（81 条数值路径 · 人眼复核指引）

> **性质**：一次性、**只读**的「改前 / 改后 `git diff -U0` 逐行对照」人工复核物（用户已批准授权）。
> **不是测试**：不恢复 `tests/`、不建 harness、不写自动断言、不引第三方依赖。
> **「不落库」口径** = 不创建任何**可被程序消费**的基线数据结构（不存 json/csv golden、不建脚本比对集）；本文件是**人读的 Markdown**，机器不读它。
> **base** = `97c1fed`（`rewrite-v3` 起点，代码内容 == `d9f6a3b`）→ **head** = `<rev>`。
> 生成：`git diff -U0 97c1fed..<rev> -- <单文件>`（逐条命令，不串联）。

---

## 0. 已知限制（如实，不放好看）

1. **只证「文本级零漂移」，不证「数值 / 时序等价」**：表能证明「某行改了 / 没改」，**不能**证明「改得对」。等价性仍只靠用户真机实跑。
2. **未覆盖 37 条无护栏项**（非 §3/§4/§10）：§1（1.3、1.8）；§2（2.4、2.8、2.9、2.11、2.16、2.19）；§5（5.1–5.6、5.11、5.12、5.19、5.33、5.35–5.37）；§6（6.1、6.14、6.15、6.18–6.22、6.24、6.25、6.28、6.33、6.39）；§7（7.1、7.19）；§8（8.29）。它们由 GUI/训练/autodl 驱动，本表**不覆盖**，回归仍只能真机暴露。
3. **`git diff -U0` 噪声**：对**纯移动 / 重排**显示为「删 + 增」，会把「只挪了位置」放大成多行变更，须人眼区分「重排（无害）」与「真改」。
4. **跨文件不显现**：若冻结函数的**调用顺序**被在另一文件里重排，本表（按文件切）不直接显现，需两侧文件同看。
5. **A-2 覆盖面缺口**：A-2 改 **3 文件 4 站点**（`postprocess.py` 两点共用 + `synthesis.py` + `engine.py`），其中 `rvc/streaming/engine.py` **不在**本 29 冻结集内（非数值冻结路径），其 `warmup(30)` 不在表内 —— 但 A-2 **值不变**，风险低，靠 B2.5 普通 diff 复核。（`rvc/core/config.py:38` 的 `crossfade_time=0.04` 系 **dataclass 字段默认值、非算法常数**，**已从 A-2 剔除**，见《目标架构方案》附录 H）
6. **R-2 仍只兜「文本」**：`rvc/runtime/graph.py` 扩入后能看「捕获 / 回放代码是否被改」，**证不了 CUDA Graph 捕获 / 回放时序等价**。

---

## 1. 覆盖文件集（共 29）

- **数值冻结（27）**：`dsp/`(4) + `nn/`(4) + `models/`(9) + `pipeline/`(7) + `streaming/`(3)
  - `pipeline/`(7)：`pipeline.py`、`features.py`、`synthesis.py`、`state.py`、`pitch/{extractor,postprocess,tracker}.py`
  - `streaming/`(3)：`runner.py`、`effects.py`、`output.py`（**无 `streaming/synthesis.py`——该文件不存在**）
- **R-2 补充冻结（2）**：`rvc/runtime/graph.py`、`rvc/runtime/device.py`（**非** §3/§4/§10 原有条目，为 §7.3 **R-2 高危点位**补入）
- **A 组「必零漂移」= 19**；**B 组「预期有变更」= 10**（净额历经 D-4 撤回、C-3/C-11 撤回、B-7 `state.py` 三项调整，合计恒为 29）。
- **D-4 撤回影响**：`rvc/pipeline/pitch/extractor.py` 原以「D-4 报错文案（~175）指向真实路径」列入 B 组；该改动**越权**（对外可见字符串，不在 §12）已撤回 → 本文件由 **B 组移入 A 组**（预期不变）。
- **C-3 / C-11 撤回 + B-7 补入**：`rvc/pipeline/pipeline.py`（C-3 波及）、`rvc/models/hubert.py`（C-11）的改动**已撤回** → 由 **B 组移入 A 组**；`rvc/pipeline/state.py` 因 B2 删死字段 `function`（B-7）由 **A 组移入 B 组**。**最终 A 19 / B 10**（合计仍 29）。

---

## 2. 逐文件复核指引

表头：`文件 | 预期变更点（行·原因·批次） | 预期不变点（逐字不动）`

### 2.1 A 组 — 必零漂移（19）※ 任何一行有 diff 即违约，立即停手核查

| 文件 | 预期变更点 | 预期不变点 |
|---|---|---|
| `rvc/dsp/hz_midi.py` | 无 | 全文 |
| `rvc/dsp/mel.py` | 无 | 全文 |
| `rvc/dsp/rms.py` | 无 | 全文 |
| `rvc/dsp/sola.py` | 无 | 全文 |
| `rvc/nn/modules.py` | 无 | ★**全文**（404 行、全项目最大、数值最密集；`.data` 为 Tensor 属性不可改——B1 已撤销该误报） |
| `rvc/nn/attentions.py` | 无 | 全文 |
| `rvc/nn/discriminator.py` | 无 | 全文 |
| `rvc/nn/vits_blocks.py` | 无 | 全文 |
| `rvc/models/rmvpe/blocks.py` | 无 | 全文 |
| `rvc/models/rmvpe/transforms.py` | 无 | 全文 |
| `rvc/models/rmvpe/constants.py` | 无 | 全文 |
| `rvc/models/synthesizer_model.py` | 无 | 全文 |
| `rvc/models/synthesizer_encoder.py` | 无 | 全文 |
| `rvc/models/synthesizer_decoder.py` | 无 | 全文 |
| `rvc/models/synthesizer_flow.py` | 无 | 全文 |
| `rvc/models/hubert.py` | 无（**C-11 撤回**：换流防护不可拆，不改） | 全文（`load_hubert` 手工换流 + `from_pretrained` 加载逻辑） |
| **`rvc/pipeline/features.py`** | 无 | ★**全文**（R-1 关键：`postprocess_features` 的 upsample + 末帧复制 + ×2 插值） |
| `rvc/pipeline/pipeline.py` | 无（**C-3 撤回**：`default_inference_cache` 全局单例不改） | ★5 阶段编排：`formant_factor` 计算（139–142）、`formant` 缩放（245–267）、阶段调用次序 |
| `rvc/pipeline/pitch/extractor.py` | 无（**D-4 撤回**：报错文案越权，不改） | 全文 |

### 2.2 B 组 — 预期有变更（10）※ 只准在「预期变更点」处变，其余逐字不动

| 文件 | 预期变更点（行·原因·批次） | 预期不变点（逐字不动） |
|---|---|---|
| `rvc/models/rmvpe/model.py` | D-5 在 `torch.load(...,weights_only=False)`（~36）**加安全注释** | 全代码（**只加注释**） |
| `rvc/pipeline/state.py` | B-7 删死字段 `function: str = "vc"`（恒 `"vc"`、分支不可达） | 其余全部字段与默认值逐字不动 |
| **`rvc/pipeline/synthesis.py`** | A-2 `>= 64`（**76**）→ 具名常量（值=64 不变）；C-8 `infer_synth_audio` 的 `skip_head is not None` 哨兵（如需） | ★`>=64` 比较语义与「整表清空」行为；`cached_long_tensor` 缓存键 |
| `rvc/pipeline/pitch/postprocess.py` | A-2 `> 128`（**79、97**）→ 具名常量（值=128 不变） | ★`apply_pitch_map`、`median_filter_f0`、`normalize_f0_to_coarse` |
| **`rvc/pipeline/pitch/tracker.py`** | D-1 函数 docstring「返回最后 p_len 帧」及理由（**26–33**）→「前 p_len 帧」 | ★**代码 54–65 逐字不动，尤其 59 `f0_raw[:p_len]`**（R-1 红线） |
| `rvc/streaming/runner.py` | B-7 删 `state.function=="vc"` 死分支（47 赋值、181、212 判定） | ★`stage_*` 调度顺序、`_stage_formant`（282–312） |
| **`rvc/streaming/effects.py`** | B-9 删过期 docstring（4、8–9）；B-6 删 `AudioProcessor.sr`（**88、92**）；B-7 删 `process_output` 的 `is_vc` 参数与 `if is_vc`（101/104/106，恒 True） | ★RMS→SOLA 调用顺序（107–108）、`apply_rms_mix`/`apply_sola` 实参 |
| `rvc/streaming/output.py` | C-9 `except Exception: pass`（29–30）→ 精确捕获 `queue.Empty` | `write_main_output`（11–16）、`route_secondary_output`（19–27、31） |
| **`rvc/runtime/graph.py`**（R-2 补） | C-2 `_active_options` 全局（38）→ **同文件内显式持有**（B3-2 已改）；`configure_cuda_graph`（54–71）签名 / 调用点不变 | ★`_GraphCache.__init__` 读 `max_entries` 的**时机与取值**（150–153）、`_rvc_cuda_graph_cache` 猴补（179–182）、`purge_cuda_graphs` —— **逐字不动**。**补注（B3-2 等价性）：写入时机 / 读取时机 / 取值 / 调用顺序「四维不变」，仅把裸模块全局换成同文件内显式持有者（文本级去全局）；「文本随去全局改写」≠ 时序改变** |
| **`rvc/runtime/device.py`**（R-2 补） | B1 `Config`→`RuntimeDeviceConfig`（12，已完成）；C-1 保留「进程内一份」语义 | ★`__new__` 单例语义、`device/is_half` 判定与无 GPU 的中文 `RuntimeError` |

---

## 3. 复核操作法（人眼）

1. 对每个文件：`git diff -U0 97c1fed..<rev> -- <单文件>`（逐条命令，不串联）。
2. **先看 A 组 19 个**：只要输出**非空** → 立即停手，逐行确认「本应零漂移」为何变了。
3. **再看 B 组 10 个**：把每个 hunk 对照本表「预期变更点」——**在预期点内** 判「一致」；**落在预期点外** 判「漂移」，必须解释。
4. 「重排型噪声」：若 hunk 是同表达式换行 / 缩进变化、**无值改动** → 判「重排（无害）」，仍须在行内注明。
5. **收口**：对 29 文件再跑一次 `base→head` 总表，确认最终态与逐批结论一致。

### 输出格式（模板 · 一眼看出哪行漂移）

汇总表：
```
## S4 · 81 条数值路径改前/改后对照表  base=97c1fed → head=<rev>
| 文件 | 冻结类别 | 变更行 | 其中代码行 | 其中注释/文档行 | 初判 |
|------|---------|-------:|----------:|----------------:|------|
| rvc/dsp/sola.py         | 数值 | 0 | 0 | 0 | 零漂移 |
| rvc/pipeline/tracker.py | 数值 | 3 | 1 | 2 | 需人眼(1 代码行) |
| …                       |      |   |   |   |      |
| **合计**                |      | N | x | y | 待判 x 行 |
```
单文件明细（仅展开变更行 ≠ 0 的文件）：
```
### rvc/pipeline/pitch/tracker.py  变更行=3（代码1/注释2）
| 新行号 | ± | 类 | 变更前 | 变更后 | 人工判定(一致/漂移/N/A) |
|-------:|---|----|--------|--------|-------------------------|
| 54 | - | 注释 | # 取最后 p_len 帧… |                    |  |
| 54 | + | 注释 |                    | # 取前 p_len 帧…   |  |
| 58 | ± | 代码 | f0_raw[-p_len:]    | f0_raw[:p_len]     |  |
```
「零漂移」形态：该文件**不展开**，仅汇总表一行 `0/0/0/零漂移`；若 29 文件全 0 → 正文一句「全部冻结文件零漂移」。

> **判据**：**文件级冻结 + 全量展示，不做行级过滤**（零漏报优先于零误报）。「类」列（注释/文档 vs 代码）为**启发式，可能误分类，以人眼为准，不作为断言**。

---

## 4. 与「49 项清单 / §②」的对账

| # | 对账链 | 判据 | 结果（D-4 / C-3 / C-11 撤回 + B-7 补入后重核） |
|---|---|---|---|
| 1 | 本表 B 组(10) ⊆ 附录 B 49 项 | 逐条命中 | ✅ |
| 2 | 本表 A 组(19) ∩ 49 项 = ∅ | 无交集 | ✅ |
| 3 | §② ⇄集 ↔ 49 项 | 双向差集为空 | ✅ |

> 详见 `docs/rewrite-v3/目标架构方案.md` 附录 E（三链机械对账）、附录 F（**D-1 / D-3 已裁定**：净效应为零、维持 81/49）与附录 H（**D-4 / C-3 / C-11 撤回**）。

---

## 5. 实测结果（阶段四 · 2026-09-23）

> base = `97c1fed` → head = `5321252`（B6 收口后）。命令：`git diff -U0 97c1fed..HEAD --stat -- <A组19文件>` 输出**为空**（A 组全零漂移）；B 组逐文件 `git diff -U0` 逐 hunk 对照 §2.2「预期变更点」。分类列（代码/注释）为启发式，以人眼复核为准。

### 5.1 汇总表

| 文件 | 冻结类别 | 变更行 | 其中代码行 | 其中注释/文档行 | 初判 |
|------|---------|-------:|----------:|----------------:|------|
| rvc/dsp/hz_midi.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/dsp/mel.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/dsp/rms.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/dsp/sola.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/nn/modules.py | A | 0 | 0 | 0 | 零漂移（404 行全文未动） |
| rvc/nn/attentions.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/nn/discriminator.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/nn/vits_blocks.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/models/rmvpe/blocks.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/models/rmvpe/transforms.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/models/rmvpe/constants.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/models/synthesizer_model.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/models/synthesizer_encoder.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/models/synthesizer_decoder.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/models/synthesizer_flow.py | A | 0 | 0 | 0 | 零漂移 |
| rvc/models/hubert.py | A | 0 | 0 | 0 | 零漂移（C-11 撤回兑现） |
| rvc/pipeline/features.py | A | 0 | 0 | 0 | 零漂移（R-1 upsample 对齐未动） |
| rvc/pipeline/pipeline.py | A | 0 | 0 | 0 | 零漂移（C-3 撤回兑现，5 阶段编排未动） |
| rvc/pipeline/pitch/extractor.py | A | 0 | 0 | 0 | 零漂移（D-4 撤回兑现） |
| rvc/models/rmvpe/model.py | B | 1 | 0 | 1 | 一致（D-5 安全注释，仅 +1 注释行） |
| rvc/pipeline/state.py | B | 1 | 1 | 0 | 一致（B-7 删死字段） |
| rvc/pipeline/synthesis.py | B | 13 | 2 | 11 | 一致（A-2 值 64 不变 + B5 docstring 对齐；C-8 未改代码） |
| rvc/pipeline/pitch/postprocess.py | B | 7 | 3 | 4 | 一致（A-2 值 128 不变，两点共用一常量） |
| rvc/pipeline/pitch/tracker.py | B | 10 | 0 | 10 | 一致（D-1 仅 docstring；`f0_raw[:p_len]` 逐字未动） |
| rvc/streaming/runner.py | B | 9 | 8 | 1 | 一致（B-7；含预期点内连带，见 5.2 明细注） |
| rvc/streaming/effects.py | B | 19 | 4 | 15 | 一致（B-9/B-6/B-7 全部命中预期行号） |
| rvc/streaming/output.py | B | 2 | 2 | 0 | 一致（C-9 纯 narrowing） |
| rvc/runtime/graph.py | B | 42 | 30 | 12 | 一致（C-2/B3-2 去全局；写 1 读 2 时机不变，R-2 补注兑现） |
| rvc/runtime/device.py | B | 2 | 2 | 0 | 一致（B1 `Config`→`RuntimeDeviceConfig`，纯改名） |
| **合计** | | **106** | **52** | **54** | **A 组全零；B 组 0 行预期点外漂移** |

### 5.2 单文件明细（仅变更行 ≠ 0 的文件）

#### rvc/models/rmvpe/model.py 变更行=1（代码0/注释1）
| 新行号 | ± | 类 | 变更前 | 变更后 | 人工判定 |
|-------:|---|----|--------|--------|----------|
| 36 | + | 注释 | | `# 安全前提：RMVPE 权重为随仓库分发的官方资产…故 weights_only=False。` | 一致（D-5） |

#### rvc/pipeline/state.py 变更行=1（代码1/注释0）
| 新行号 | ± | 类 | 变更前 | 变更后 | 人工判定 |
|-------:|---|----|--------|--------|----------|
| — | − | 代码 | `function: str = "vc"`（:43） | | 一致（B-7 死字段，恒 `"vc"`） |

#### rvc/pipeline/synthesis.py 变更行=13（代码2/注释11）
| 新行号 | ± | 类 | 变更前 | 变更后 | 人工判定 |
|-------:|---|----|--------|--------|----------|
| 7–8 | + | 注释 | | `# formant 重采样核缓存上限…` + 空行 | 一致（A-2） |
| 8 | + | 代码 | | `_RESAMPLE_KERNEL_LIMIT = 64` | 一致（值=64 不变） |
| 39–41 | −/+ | 注释 | docstring「（实时/离线共用）…离线路径不传…5 参签名…」 | docstring「唯一调用路径恒传 skip_head…offline 分支不可达保留」 | 一致（B5 docstring 对齐，与 C-8 撤回结论一致） |
| 79 | ± | 代码 | `if len(resample_kernel) >= 64:` | `if len(resample_kernel) >= _RESAMPLE_KERNEL_LIMIT:` | 一致（A-2，阈值语义不变） |

#### rvc/pipeline/pitch/postprocess.py 变更行=7（代码3/注释4）
| 新行号 | ± | 类 | 变更前 | 变更后 | 人工判定 |
|-------:|---|----|--------|--------|----------|
| 23–25 | + | 注释+代码 | | `# 音域映射缓存上限…` + `_PITCH_MAP_CACHE_LIMIT = 128` + 空行 | 一致（A-2，值=128 不变） |
| 82 | ± | 代码 | `if len(_pitch_map_midi_cache) > 128:` | `> _PITCH_MAP_CACHE_LIMIT` | 一致 |
| 100 | ± | 代码 | `if len(_pitch_map_tensor_cache) > 128:` | `> _PITCH_MAP_CACHE_LIMIT` | 一致（两点共用一常量） |

#### rvc/pipeline/pitch/tracker.py 变更行=10（代码0/注释10）
| 新行号 | ± | 类 | 变更前 | 变更后 | 人工判定 |
|-------:|---|----|--------|--------|----------|
| 26 | ± | 注释 | `"""…返回最后 p_len 帧。` | `"""…返回前 p_len 帧。` | 一致（D-1） |
| 30–33 | −/+ | 注释 | 「不再丢弃边缘帧！…取最后 p_len 帧…」4 行 | 「取前 p_len 帧是特征侧对齐要求…」4 行 | 一致（B5 论证改写）；**代码 54–65 未出现于 diff（逐字未动），R-1 红线守住** |

#### rvc/streaming/runner.py 变更行=9（代码8/注释1）
| 新行号 | ± | 类 | 变更前 | 变更后 | 人工判定 |
|-------:|---|----|--------|--------|----------|
| 41 | ± | 代码 | `def __init__(self, pipeline, runtime_params, function: str = "vc"):` | `def __init__(self, pipeline, runtime_params):` | **一致（预期点内连带）**：形参唯一用途是喂 :47 死赋值，删死分支必连带删形参；调用方 engine.py 已同步（B1/B2.5/B5 触及文件） |
| 46 | − | 代码 | `self.state.function = function`（:47） | | 一致（B-7） |
| 180 | ± | 代码 | `if state.function == "vc" and self.pipeline:`（:181） | `if self.pipeline:` | 一致（恒真子件删除） |
| 210–211 | ± | 代码 | `process_output(infer, ref, p_rms_mix, state.function == "vc",)` | `process_output(infer, ref, p_rms_mix)` | 一致（实参=恒 True 表达式删除）；**RMS→SOLA 序未触及** |

#### rvc/streaming/effects.py 变更行=19（代码4/注释15）
| 新行号 | ± | 类 | 变更前 | 变更后 | 人工判定 |
|-------:|---|----|--------|--------|----------|
| 4 | ± | 注释 | `…→ RMS 混合 → SOLA → 清辅音保护` | `…→ RMS 混合 → SOLA` | 一致（B-9） |
| 6–8 | − | 注释 | 清辅音保护/pitchf_cache 段落 3 行 | | 一致（B-9，功能已不存在） |
| 79 | ± | 注释 | 类 docstring `…→ SOLA → 清辅音保护` | `…→ SOLA` | 一致（B-9） |
| 84, 87 | − | 代码 | `self.sr = 48000`（:88）、`self.sr = sr`（:92） | | 一致（B-6 死成员） |
| 96–99 | ± | 代码 | `def process_output(self, infer, ref, rms_mix, is_vc=True):` + docstring + `if is_vc:` 缩进 | 3 参签名 + 单行 docstring + `infer = self.rms_mix.process(...)` 直排 | 一致（B-7 恒 True 分支删除）；**RMS→SOLA 调用顺序与实参逐字未动** |

#### rvc/streaming/output.py 变更行=2（代码2/注释0）
| 新行号 | ± | 类 | 变更前 | 变更后 | 人工判定 |
|-------:|---|----|--------|--------|----------|
| 29 | ± | 代码 | `except Exception:` | `except queue.Empty:` | 一致（C-9 纯 narrowing；异常路径语义仅收窄） |

#### rvc/runtime/graph.py 变更行=42（代码30/注释12）
| 新行号 | ± | 类 | 变更前 | 变更后 | 人工判定 |
|-------:|---|----|--------|--------|----------|
| 14 | ± | 注释 | docstring `device.Config` | `device.RuntimeDeviceConfig` | 一致（B1 改名同步） |
| 36–55 | −/+ | 代码+注释 | 裸全局 `_active_options: RuntimeOptions \| None = None`（含 2 行注释） | `_ActiveGraphOptions` 类（`__slots__` + `apply()` 唯一写入口 + `get()`）+ `_active_options = _ActiveGraphOptions()` | 一致（C-2/B3-2：结构承载「进程内单点」契约；S4 §2.2 补注「文本随去全局改写 ≠ 时序改变」兑现） |
| 76 | ± | 注释 | `本函数是 _active_options 的唯一写入口` | `本函数是生效选项的唯一写入口` | 一致 |
| 77, 84–87 | ± | 代码 | `global _active_options` 删除；`_active_options = RuntimeOptions(...)` | `_active_options.apply(RuntimeOptions(...))` | 一致（写点数量=1 不变；写时机=configure_cuda_graph 调用点不变） |
| 93–95 | ± | 代码 | `_active_options is not None and _active_options.use_cuda_graph` | `opts = _active_options.get()` + `opts is not None and opts.use_cuda_graph` | 一致（读点 1/2；读取位置不变，先取局部再判等价） |
| 169–171 | ± | 代码 | `_active_options.graph_cache_size if _active_options is not None` | `opts = _active_options.get()` + 同构判断 | 一致（读点 2/2；**`_GraphCache.__init__` 读 max_entries 的时机与取值不变**） |
| — | （无 hunk） | — | `_rvc_cuda_graph_cache` 猴补（旧 179–182）、`purge_cuda_graphs` | | **逐字未动**（未出现在 diff 中）✅ |

#### rvc/runtime/device.py 变更行=2（代码2/注释0）
| 新行号 | ± | 类 | 变更前 | 变更后 | 人工判定 |
|-------:|---|----|--------|--------|----------|
| 12 | ± | 代码 | `class Config:` | `class RuntimeDeviceConfig:` | 一致（B1 纯改名；单例语义/无 GPU 中文 RuntimeError 未触及） |

### 5.3 收口结论

1. **A 组 19/19 零漂移**（一条 diff 都没有）——`dsp/`、`nn/`、`models/` 数值主体与 `features.py`/`pipeline.py`/`extractor.py` 全部逐字保持。
2. **B 组 10/10 一致**：全部变更落在 §2.2 预期变更点内；唯一需注明的是 `runner.py` `__init__` 签名删参（预期点内必要连带）与 `graph.py` 去全局结构变换（D-R4 四维不变已逐点核对：写点 1、读点 2、写入时机、读取时机均不变）。
3. **R-1 红线**：`f0_raw[:p_len]`（tracker 代码 54–65）零触碰；**R-2 红线**：`_GraphCache` 读 `max_entries` 时机/取值、猴补、`purge_cuda_graphs` 零触碰。
4. 本表性质不变（§0）：只证文本级零漂移，**数值/时序等价仍以用户真机实跑为最终判定**。
