# 功能基线清单（对外可观测行为）— 第三轮重写

> 阶段一产物 · 基线 `master@d9f6a3b`（工作区干净）。
> **唯一依据 = 当前代码。** 上一轮（2026-09-16）的《功能基线清单》仅作线索，已被两笔提交改变：
> `f800ec3`（F0/HuBERT 输入对齐改造，删除 pitch/confidence 滚动缓存，删除 `tests/`）、
> `d9f6a3b`（深度 review 修复）。凡旧文档与代码冲突，一律以代码为准。
>
> **粒度**：每条 = 一条可独立验证的断言（「给定 X，期望 Y」），后续重写每条实现必须能被判定为「一致」或「违约」。
> **验证方式枚举**（只能取此四值）：
> - `[对拍]` 冻结旧实现跑确定性输入，与新实现逐样本/逐块比对（音频、DSP、数值后处理）
> - `[S]` 可静态断言（AST / 函数签名 / 常量值 / dataclass 字段 / 导入图）
> - `[D]` 需构造确定性输入后 diff（非音频类：配置字节、日志行、退出码、产物文件清单）
> - `[M]` 只能手工真机验证（GPU / 麦克风 / 扬声器 / 人耳主观 / Win32 托盘）
>
> **⚠ 硬约束**：`tests/` 已被用户有意删除且明确不恢复。**任何条目都不允许写「已有测试覆盖」或「复用测试」。**
> **范围**：`Retrieval-based-Voice-Conversion-WebUI/` 为 gitignore 的原版参考副本，零引用，不在基线内。
> **`README.md` 已知失真**（测试章节、`rvc/streaming/loudness.py`、`rvc/train/data_utils.py`、`rvc/streaming.VoiceEngine` 均不存在此名），**不可尽信**。

---

## 1. 入口与命令行

| # | 行为断言 | 证据(文件:行) | 验证方式 | 护栏 | 备注 |
|---|---|---|---|---|---|
| 1.1 | 给定无参数运行 `python app.py`，argparse 因互斥且必需的 `--infer`/`--train` 均缺失而报错并以退出码 2 结束 | `app.py:38-42` | `[D]` | — | `required=True` 的 mutually_exclusive_group |
| 1.2 | 给定 `--infer --train` 同时传入，argparse 报「not allowed with」并以退出码 2 结束 | `app.py:39-41` | `[D]` | — | |
| 1.3 | 给定 `python app.py --infer`，构造 `gui.infer.view.window.MainWindow`；给定 `--train`，构造 `gui.train.view.window.TrainWindow`；两者二选一后 `win.show()` 并 `sys.exit(app.exec())`（退出码 = Qt 事件循环返回码） | `app.py:50-58` | `[S]`+`[M]` | ▲ 无护栏 | 导入在分支内惰性发生 |
| 1.4 | 进程启动即在模块 import 期把 `sys.stdout`/`sys.stderr` 重新包装为 UTF-8、`errors="replace"`；`sys.stdout is None` 时跳过（不抛） | `app.py:13-15` | `[D]` | — | 包装发生在 argparse 之前 |
| 1.5 | 日志 `basicConfig(level=DEBUG, format="%(message)s", force=True)`，handler 为 `StreamHandler(stream=sys.stdout)`（仅当 `sys.stdout` 非 None，否则 handlers 为空列表）；并把 `torchfcpe`/`numba`/`matplotlib` 三个 logger 降为 `WARNING` | `app.py:17,20-28` | `[S]`+`[D]` | — | `force=True` 覆盖既有 handler |
| 1.6 | logging 在模块顶层 import 期就配置完成（`_configure_logging()` 在 `if __name__` 之外调用） | `app.py:31` | `[S]` | — | 重写时若挪进 `main()` 即违约（子模块 import 期日志格式变化） |
| 1.7 | app.py 把自身所在目录插入 `sys.path[0]`（`now_dir = dirname(abspath(__file__))`） | `app.py:33-34` | `[S]` | — | |
| 1.8 | `start-infer.bat`：`cd /d "%~dp0"` → `.venv\Scripts\python.exe app.py --infer` → 空行 + `==== exited, code: %errorlevel% (non-zero = see errors above) ====` → `pause`；`start-train.bat` 同构但传 `--train` | `start-infer.bat:1-6`、`start-train.bat:1-6` | `[M]` | ▲ 无护栏 | |
| 1.9 | `python autodl_train.py` 与 `python -m autodl` 等价：薄包装 `sys.exit(main())`，`main` 来自 `autodl.__main__` | `autodl_train.py:6-11`、`autodl/__main__.py:124-125` | `[D]` | — | |
| 1.10 | `autodl.__main__.main()` **第一行** `os.chdir(PROJECT_ROOT)`，因此任意 cwd 启动后 cwd 变为项目根（`import` 期不 chdir） | `autodl/__main__.py:29`、`autodl/__init__.py:11` | `[D]` | — | |
| 1.11 | autodl 退出码语义：`EXIT_OK=0`（成功 / 用户取消 / 训练前拒绝）、`EXIT_ENV=1`（环境体检致命缺失）、`EXIT_RUNTIME=2`（运行异常 / KeyboardInterrupt / 二次终止信号强退） | `autodl/__init__.py:47`、`autodl/__main__.py:49,62,99,108,111,119`、`autodl/steps.py:25` | `[D]` | — | |
| 1.12 | autodl 只取第一个不以 `-` 开头的 argv 作为数据集路径（`argv_dir`，供向导 Q1 作默认值）；无参时为 `""` | `autodl/__main__.py:35-38` | `[D]` | — | |
| 1.13 | autodl 启动即挂 `DATA_ROOT/logs/autodl_train.log`；实验名确定后 `reattach` 到 `<exp_dir>/autodl_train.log`：**删除旧文件**，把旧内容以 `—— 以下为实验名确定前的体检记录 ——` / `——————————————————` 包裹写入新文件头部 | `autodl/__main__.py:42`、`autodl/wizard.py:53`、`autodl/logger.py:29-48` | `[D]` | — | |
| 1.14 | 日志文件行格式固定 `[MM-DD HH:MM:SS] [LEVEL ] msg\n`（level 左对齐宽度 5，带一尾随空格）；未 attach 时 `log()` 只裸打印不进文件，并暂存 buffer，attach 时回放 | `autodl/logger.py:50-64` | `[D]` | — | |
| 1.15 | `log.plain()` 只上屏不进文件；`section(title)` 输出 `BAR` + `【title】` + `BAR`，其中 `BAR = "─" * 66` | `autodl/logger.py:73-80`、`autodl/__init__.py:48` | `[D]` | — | |
| 1.16 | `file_only()` 只写文件不上屏（Trainer 自带 epoch 行与 `EPOCH` 行去重用） | `autodl/logger.py:66-71`、`autodl/steps.py:217-221` | `[D]` | — | |
| 1.17 | autodl 在 main 内静音 `lr_scheduler.step()` 相关 RuntimeWarning（`warnings.filterwarnings`），不改源码 | `autodl/__main__.py:33` | `[S]` | — | |
| 1.18 | `autodl.__init__` import 期对 stdout/stderr 做 `reconfigure(encoding="utf-8", errors="replace")`，失败静默忽略 | `autodl/__init__.py:15-19` | `[S]` | — | |

## 2. rvc 引擎公开 API（`rvc.streaming.engine.VoiceEngine`）

> ⚠ 正确导入路径是 `rvc.streaming.engine.VoiceEngine`；`rvc/streaming/__init__.py` **只含 docstring，不导出任何名字**，故 `rvc.streaming.VoiceEngine` 不存在（旧文档有误）。

| # | 行为断言 | 证据(文件:行) | 验证方式 | 护栏 | 备注 |
|---|---|---|---|---|---|
| 2.1 | `VoiceEngine(runtime_params, inference_cache=None, on_runtime_error=None)` 构造后公开属性初值：`running=False`、`function="vc"`、`infer_ms=0.0`、`measure_ms=0.0`、`pth_path=""`、`sr_model=0`、`sr_dev=0`、`pipeline=None` | `engine.py:34-54` | `[S]` | — | |
| 2.2 | `error_count`/`last_error`/`runtime_error_pending` 在 `_runner is None` 时分别返回 `0`/`""`/`False`（不抛）；`runtime_error_pending` 有 setter，`_runner` 为 None 时静默忽略 | `engine.py:58-83` | `[S]` | — | |
| 2.3 | `input_pitch` 只读属性：`_runner is None` 返回 `0.0`，否则返回 `float(state.last_input_pitch)`（音域映射之前的原始值） | `engine.py:66-74` | `[S]` | — | GUI 遥测唯一取数口 |
| 2.4 | `load_model(pth, force=False, hubert="chinese")` **无条件先**对 `(self.inference_cache or default_inference_cache)` 调 `clear_f0_cuda_graph_caches()`，再判缓存 | `engine.py:87-98` | `[S]`+`[M]` | ▲ 无护栏 | 缓存命中也会清 F0 图（多一次开销，现状保留） |
| 2.5 | `load_model` 在 `force=False 且 self.pipeline 存在 且 self.pth_path == pth` 时直接返回 `self.pipeline.target_sr`（不重建 pipeline；`inference_cache` 取值顺序不变，仍已清图） | `engine.py:97-98` | `[S]` | — | |
| 2.6 | `load_model` 正常返回 `InferencePipeline.target_sr`（= 模型 bundle 的 `target_sr`）；异常时 `logger.error(exc_info=True)`、`self.pipeline=None`、原异常**原样 re-raise**（不包装成 ModelLoadError——ModelLoadError 在 `ModelSessions.load` 内抛出） | `engine.py:99-109`、`sessions.py:74-75` | `[S]` | — | |
| 2.7 | `setup(sr_type, in_dev, out_dev, block_t, cf_t, extra_t, out2_dev_idx=None)`：若已有流先 `stop()`；`sd.default.device = [in_dev, out_dev]`；`sr_dev = int(sd.query_devices(in_dev)["default_samplerate"])`；`sr = target_sr if sr_type=="sr_model" else sr_dev` | `engine.py:113-120` | `[S]` | — | `sr_type` 非法值（非 `"sr_model"`）一律走 `sr_dev` 分支 |
| 2.8 | `setup` 调用 `validate_and_log_devices`（输入/输出 `max_*_channels<=0` 抛 `AudioDeviceError`；`channels = min(in_max, out_max, 2)`），然后建 runner（`reset_buffers=True`）→ 启动主流 → `running=True`；`out2_dev_idx` 非 None 时再调 `setup_out2` | `engine.py:122-138`、`stream.py:46-78` | `[S]`+`[M]` | ▲ 无护栏 | |
| 2.9 | 实时启动固定预热 **30** 次（静音块）后清空 48k/16k 缓冲与 SOLA 状态（`reset_buffers`） | `engine.py:155-161`、`runner.py:113-140` | `[S]`+`[M]` | ▲ 无护栏 | 30 是「首次即流畅」的刻意取值 |
| 2.10 | `setup_out2(dev_idx)`：`_runner is None` 抛 `RuntimeError("请先启动主引擎再设置副输出")`；否则启副输出流 | `engine.py:163-169` | `[S]` | — | |
| 2.11 | `stop()`：`running=False` → 若 runner 存在则 `reset_error_state()` → `stop_all()`（abort+close，吞一切异常）→ 若 `torch.cuda.is_available()` 则 `torch.cuda.synchronize()` | `engine.py:171-179`、`stream.py:183-203` | `[S]`+`[M]` | ▲ 无护栏 | 同步是为快速 stop→start 时旧 kernel 已结束 |
| 2.12 | 音频回调 `_cb(indata,outdata,frames,times,status)` 在 PortAudio 线程内同步执行（无自建推理线程）：先算 `d = times.outputBufferDacTime - times.inputBufferAdcTime`，仅当 `0 < d < 2` 才更新 `measure_ms = d*1000`（单位 ms） | `engine.py:183-190` | `[S]` | — | 负值或 >2s 忽略（设备切换防抖动） |
| 2.13 | `_cb` 正常路径：`process_block` → `infer_ms = state.infer_ms` → 若 `enable_out2` 则 `route_secondary_output` → `acknowledge_success()`（清零 `error_count`） | `engine.py:192-201` | `[S]` | — | |
| 2.14 | `_cb` 异常路径：`handle_error`（累加 error_count、记 last_error）→ **`logger.error`** → `outdata[:] = 0`（整块清零继续）→ 若 `should_stop`（error_count 达 `max_error_count=3` 且此前未 pending）则 `running=False`、回调 `on_runtime_error(last_error)`、抛 `sd.CallbackStop` | `engine.py:202-211`、`state.py:103-109` | `[M]` | ▲ 无护栏 | ⚠ 吞异常 + 清零输出继续跑是现状行为，见 §10 |
| 2.15 | `process_file(task, *, pad_sec=3.0, progress_cb=None)`：先 `update_from(task)` 原地更新 `runtime_params`（不换对象引用）、`function="vc"`、从 `task.buffer` 读 block/crossfade/extra、重建 runner（`reset_buffers=False`），返回裁剪后的输出数组**并**把结果写入 `task.output_path` | `engine.py:215-244` | `[对拍]` | ▲ 无护栏 | ⚠ 会替换 `self._runner` 并原地改 `runtime_params`——实时运行中不得调用，见 §10 |
| 2.16 | `process_file` 的 pad 与分块：`np.pad(wav, (pad,pad), mode="reflect")`，`pad=int(tgt_sr*pad_sec)`；长度不整除块长时在尾部补零到块长整数倍；最后返回 `concat(out_chunks)[pad : pad+len(wav)]` | `engine.py:253-268` | `[对拍]` | ▲ 无护栏 | reflect pad 要求输入长度 >1 |
| 2.17 | `process_file` 写盘：结果峰值 `audio_max = max(abs(result))/0.99`，`audio_max>1` 才整体除以 `audio_max`；**输出 WAV subtype 恒为 `"FLOAT"`**；空结果直接写 FLOAT 空 WAV | `engine.py:270-281`、`wav_file.py:41-44` | `[D]` | — | 文件名/字节可由确定性输入 diff |
| 2.18 | `process_file` 每块调用 `progress_cb(i+1, total_blocks)`（1 基）；无回调时静默 | `engine.py:266-267` | `[S]` | — | |
| 2.19 | 副输出路由：`out2_q` 为 `queue.Queue(maxsize=10)`；满时丢最旧（`get_nowait` 吞异常）再 `put_nowait(outdata.copy())`；**主输出不受影响**；副输出流读空队列时输出全 0 | `stream.py:30`、`output.py:19-31`、`stream.py:159-166` | `[S]`+`[M]` | ▲ 无护栏 | 副输出可跳帧 |
| 2.20 | 副输出启用派生规则：`enable_out2 = bool(output2_device) and output2_device != "不使用"`（仅由 `params_from_dict`/`collect_params` 计算，非持久化权威值） | `bindings.py:97,224` | `[S]` | — | 持久化文件里 `enable_out2` 字段存在但反向加载时被重算覆盖 |

## 3. 实时推理链（五阶段行为级契约）

| # | 行为断言 | 证据(文件:行) | 验证方式 | 护栏 | 备注 |
|---|---|---|---|---|---|
| 3.1 | `init_processing` 计算参数：`zc = sr//100`；`block_samples = round(block_t*sr/zc)*zc`；`crossfade_samples = round(cf_t*sr/zc)*zc`；`sola_buffer_samples = min(crossfade_samples, 4*zc)`；`sola_search_samples = zc`；`extra_samples = round(extra_t*sr/zc)*zc`（全部对齐 10ms 边界） | `runner.py:69-80` | `[S]`+`[对拍]` | ▲ 无护栏 | |
| 3.2 | 派生长度：`block_samples_16k = 160 * block_samples // zc`；`skip_head = extra_samples // zc`；`return_length = (block_samples + sola_buffer_samples + sola_search_samples) // zc` | `runner.py:82-84` | `[S]` | ▲ 无护栏 | 160 来自 `HUBERT_FRAME_SIZE` |
| 3.3 | 滚动缓冲区总长：48k 为 `n = extra_samples + crossfade_samples + sola_search_samples + block_samples`；16k 为 `160 * n // zc`；均 `torch.zeros` 于 `state.device` | `runner.py:86-93` | `[S]` | ▲ 无护栏 | |
| 3.4 | 重采样器：48k→16k 恒建（`TatResample(sr, 16000)`）；模型→48k 仅当 `sr_model != sr` 时建，否则为 `None` | `runner.py:98-105` | `[S]` | ▲ 无护栏 | |
| 3.5 | 输入上传：`mono = indata.mean(axis=1)`（多通道取均值）→ `np.ascontiguousarray` → 写 pinned `in_pin[:n]` → **同步**（`non_blocking=False`）拷到 `input_gpu`；`input_gpu` 容量不足才重分配 | `runner.py:226-245` | `[对拍]` | ▲ 无护栏 | 同步拷贝是刻意修复（异步会读到污染输入致 NaN） |
| 3.6 | `_stage_preprocess` 双缓冲左移：16k 与 48k 各自 `work[:-N].copy_(buf[N:])` + `work[-N:].zero_()` + 交换指针；随后把 `mono` 写入 `input_wav_48k` 尾部 | `runner.py:260-269` | `[对拍]` | ▲ 无护栏 | 16k 左移先做，与 48k 互不依赖 |
| 3.7 | 16k 重采样只处理「新块 + `2*hz_centis` 上下文」：`resampler_out = resampler_48k_to_16k(input_wav_48k[-len(mono)-2*hz_centis:])[160:]`，然后**只写** `input_wav_16k[-block_samples_16k:] = resampler_out[-block_samples_16k:]`（不写修正帧） | `runner.py:272-278` | `[对拍]` | ▲ 无护栏 | ⚠ 丢弃修正帧是刻意行为（写回会导致特征漂移） |
| 3.8 | 每块开始 `ctx.reset(self.runtime_params)`，执行顺序固定：`stage_input` → `stage_preprocess` → (`extract_features` → `postprocess_features`) → `synthesize_audio` → (模型→48k 重采样) → `_stage_formant` → `effects.process_output` → `write_main_output` | `runner.py:175-216`、`pipeline.py:109-177` | `[S]`+`[对拍]` | ▲ 无护栏 | |
| 3.9 | `function != "vc"` 或 `pipeline is None` 时 `feats=None`，直通模式：`infer = input_wav_48k[extra_samples:].clone()`，且 `process_output(..., is_vc=False)` 跳过 RMS 混合（SOLA 仍走） | `runner.py:181-213`、`effects.py:101-108` | `[对拍]` | ▲ 无护栏 | |
| 3.10 | `state.infer_ms` 每块末由 `time.perf_counter()` 差值 ×1000 更新 | `runner.py:170,218` | `[S]` | ▲ 无护栏 | |
| 3.11 | formant 因子在 `extract_features` 内计算：`factor = pow(2, formant/12)` 存 `ctx.formant_factor`；`ctx.return_length2 = ceil(return_length * factor)`；**不在输出侧重算** | `pipeline.py:139-143`、`runner.py:300-303` | `[S]` | ▲ 无护栏 | |
| 3.12 | HuBERT 特征提取输入是**完整** `state.input_wav_16k`；`_hubert_forward = model(x).last_hidden_state`；预处理（dtype/view）在图外，前向走 `run_cuda_graph(model, "hubert", ...)` | `features.py:11-36`、`pipeline.py:198-205` | `[S]`+`[对拍]` | ▲ 无护栏 | |
| 3.13 | 特征上采样：先**重复最后一帧**（`cat((feats, feats[:,-1:,:]), dim=1)`），再 `F.interpolate(scale_factor=2)`（最近邻，permute 到 C 维）→ 100fps；结果按 `is_half` 转 half/float | `features.py:55-59` | `[对拍]` | ▲ 无护栏 | |
| 3.14 | `postprocess_features` 先 upsample 得实际长度并 `ctx.p_len = feats.shape[1]`，**再**做 F0 提取，使 F0 帧数与 upsample 后特征严格一致 | `pipeline.py:168-174` | `[S]`+`[对拍]` | ▲ 无护栏 | f800ec3 的对齐改造核心 |
| 3.15 | F0 原始提取：**每块从完整 `input_wav_16k` 重新提取**（`extractor.extract_raw(input_wav, 16000)`），**不使用任何滚动缓存**；取**前 `p_len` 帧**；帧数不足则尾部补零；不做任何后处理 | `tracker.py:16-67`、`pipeline.py:207-239` | `[对拍]` | ▲ 无护栏 | ⚠ pitch_cache/pitchf_cache/confidence_cache 及其接口已全部删除，重写不得复活 |
| 3.16 | F0 提取器跨块复用：`state.f0_extractor` 为 None 或 `state.f0_extractor_method != method` 时才经 `create_f0_extractor` 重建；`use_f0 == 0` 时直接 return 不建 | `pipeline.py:216-226` | `[S]` | ▲ 无护栏 | |
| 3.17 | `postprocess_f0` 顺序固定：`confidence` 先做 kernel=3 因果中值滤波 → `apply_pitch_map`（音域映射，**始终生效**）→ `f0` 做 kernel=3 因果中值滤波 → `normalize_f0_to_coarse`；`confidence is None` 时用 `(f0>0).float()` | `extractor.py:100-123` | `[对拍]` | ▲ 无护栏 | |
| 3.18 | `pitchf` 额外乘 `ctx.return_length2 / state.return_length` 作 formant 缩放；返回 `(pitch[None,:], pitchf[None,:])`；`use_f0==0` 返回 `(None, None)` | `pipeline.py:267-270,252-253` | `[S]`+`[对拍]` | ▲ 无护栏 | |
| 3.19 | 输入音高遥测：`state.last_input_pitch = float(f0_raw[f0_raw>0].mean())`，**仅当存在非零帧时更新**（无浊音帧则保留上一次值） | `pipeline.py:255-259` | `[S]` | ▲ 无护栏 | |
| 3.20 | 中值滤波（`median_filter_f0`）：非 1-D / 非 tensor / `numel<kernel` / `kernel<3` 一律**原样返回**；否则左侧 replicate 填充 `kernel-1`；清音帧（`f0<=0`）保持 0；浊音帧若被滤成 0 则**恢复原值** | `postprocess.py:152-161` | `[对拍]` | ▲ 无护栏 | |
| 3.21 | 音域映射（`apply_pitch_map`）：参数各取 `max(x, 1.0)`；`src_min>=src_max` 或 `dst_min>=dst_max` 时**原样返回**；在 MIDI 半音尺度做线性映射（`ratio=(f0_m-src_min_m)/src_range`，线性外推**不钳制**）；`src_range<1e-6` 原样返回；输出 NaN/±inf 与 UV 帧统一置 0 | `postprocess.py:58-128` | `[对拍]` | ▲ 无护栏 | |
| 3.22 | 音域映射缓存：MIDI 常量缓存与 (参数,设备) tensor 缓存均无锁、上限 **128**，超限 `pop(next(iter(...)), None)` 淘汰最旧 | `postprocess.py:27,32,79-80,97-98` | `[S]` | ▲ 无护栏 | |
| 3.23 | 离散化（`normalize_f0_to_coarse`）：NaN/inf → 0；`f0_mel = 1127*ln(1+f0/700)`；仅 `f0_mel>0` 的帧线性映射到 `[PITCH_MIN=1, PITCH_MAX=255]` 并 `round().long()`；静音/UV（f0<=0）→ **1**（非 0） | `postprocess.py:16-19,131-133,164-194` | `[对拍]` | ▲ 无护栏 | ⚠ 注释称 0=UV，实现却是 1，重写需保持实现 |
| 3.24 | 合成器调用（`infer_synth_audio`）：实时路径传 8 参（含 `skip_head/return_length/return_length2`），离线路径传 5 参；`use_f0==1` 时 `pitch.long(), pitchf.half()/float()`，否则不传 pitch；graph key 实时带 `-rl2-{return_length2}` 后缀、离线不带；`mode` 为 `realtime`/`offline` | `synthesis.py:23-67` | `[S]`+`[对拍]` | ▲ 无护栏 | |
| 3.25 | 合成器保留原版魔数（不得「顺手化简」）：latent 噪声尺度 **0.66666**；实时 flow 上下文 **24** 帧（`flow_head=max(head-24,0)`、`dec_head=head-flow_head`） | `synthesizer_model.py:178,186,198` | `[S]` | ▲ 无护栏 | |
| 3.26 | `_stage_formant`：`formant==0` 直接做长度对齐；否则 `upp_res=int(floor(factor*work_sr//100))`，仅当 `upp_res != work_sr//100` 才 `apply_formant_resample(infer[:return_length*upp_res], ...)`；最后统一对齐到 `expected = block_samples + sola_buffer_samples + sola_search_samples`（长截断、短右补零） | `runner.py:296-317`、`synthesis.py:70-83` | `[对拍]` | ▲ 无护栏 | |
| 3.27 | formant 重采样核按 `upp_res` 缓存于 `state.resample_kernel`，**≥64 时整表 `clear()`**（非淘汰单个） | `synthesis.py:75-77` | `[S]` | ▲ 无护栏 | |
| 3.28 | RMS 混合（`apply_rms_mix`）：`rms_mix >= 1.0` 时跳过；否则 `converted * (r1/r2)^(1-rms_mix)`，其中 `r1 = fast_rms(ref[:len(conv)], 4*hz, hz)`、`r2 = fast_rms(conv, 4*hz, hz)` 各经 `F.interpolate(size=N+1, align_corners=True)[:, :-1]` 对齐；`r2` 钳下限 `1e-3`；`ref_hz` 缺省等于 `hz_per_centisecond` | `rms.py:39-69`、`effects.py:32-35` | `[对拍]` | ▲ 无护栏 | |
| 3.29 | `fast_rms`：reflect 填充 `frame_length//2`；输入长度 `<= padding` 抛 `ValueError`（含中文提示）；输出 `sqrt(clamp(avg_pool1d(..., frame_length, hop) , min=1e-8))` | `rms.py:13-36` | `[对拍]` | ▲ 无护栏 | |
| 3.30 | SOLA（`apply_sola`）：取前 `sola_buffer_samples+sola_search_samples` 与 `sola_buffer` 做 conv1d 互相关，**只除以 `sqrt(当前块能量+1e-8)`**，`argmax` 得整型 offset；`infer = infer[offset:]`；`sola_buffer` 或待淡化区含 NaN 时 `sola_buffer.zero_()` 并返回 `infer[:block_samples].clone()`；否则 `infer[:sola_buffer_samples] = infer*fade_in + sola_buffer*fade_out`，`sola_buffer[:] = infer[block_samples:block_samples+sola_buffer_samples]`（新尾若含 NaN 则清零），返回 `infer[:block_samples]` | `sola.py:23-76` | `[对拍]` | ▲ 无护栏 | ⚠ 返回值与入参共享存储且已被原地改写；**禁止跨块 offset 粘滞平滑** |
| 3.31 | SOLA 窗：`ls = linspace(0,1,steps=sola_buffer_samples)`；`fade_in = sin(0.5*pi*ls)^2`；`fade_out = 1 - fade_in`；`sola_norm_kernel = ones(1,1,sola_buffer_samples)` | `effects.py:61-65` | `[对拍]` | ▲ 无护栏 | |
| 3.32 | 主输出写入：`channels==1` 时 `outdata[:,0] = chunk`，否则 `outdata[:] = chunk[:,None]`（广播到所有通道） | `output.py:11-16` | `[对拍]` | ▲ 无护栏 | |
| 3.33 | CUDA Graph 缓存：每 owner 一个 `_GraphCache`，默认上限 **8**；key = `(namespace,) + 每个输入的 (shape, stride, dtype, device, requires_grad)` 签名；捕获前 3 次 warmup 前向；回放前 `static.copy_(value, non_blocking=True)` 并等待上次 `done_event` | `graph.py:26,91-98,101-137,155-172` | `[S]`+`[M]` | ▲ 无护栏 | |
| 3.34 | CUDA Graph 开关**不再来自环境变量**：`Config` 构造时用 `RuntimeOptions(use_cuda_graph=True, graph_cache_size=8)` 调 `configure_cuda_graph`（唯一写入口）；仅当 `use_cuda_graph and device 类型=="cuda" and torch.cuda.is_available()` 才启用 | `graph.py:29-70`、`device.py:31-39` | `[S]` | ▲ 无护栏 | ⚠ `RVC_CUDA_GRAPH`/`RVC_CUDA_GRAPH_MAX_CACHE` 环境变量通道已删除，重写不得恢复 |
| 3.35 | `purge_cuda_graphs(*owners)`：摘下各 owner 缓存 → 逐缓存取锁（等待在途回放）→ 清 entries → 一次 `gc.collect()` + `torch.cuda.synchronize()` + `torch.cuda.empty_cache()`；`clear_cuda_graph_cache(owner)` 为其单 owner 形式 | `graph.py:186-227` | `[S]`+`[M]` | ▲ 无护栏 | |
| 3.36 | 设备初始化：`Config.__new__` 单例（双检锁）；`device="cuda:0"`、`is_half=True`、`use_cuda_graph` 由探测决定；`gpu_mem = int(total_memory/1024/1024/1024 + 0.4)`（**+0.4 取整**） | `device.py:12-56` | `[S]` | ▲ 无护栏 | |
| 3.37 | 无 CUDA 时 `Config._init_device` 抛 `RuntimeError`（中文可读文案，提示装驱动/CUDA 版 PyTorch），**不做 CPU 回退** | `device.py:41-49` | `[S]` | ▲ 无护栏 | 与 autodl 的 CPU 降级相反（见 8.6） |
| 3.38 | 模型缓存槽位（`InferenceCache`，全局单例 `default_inference_cache`）：hubert LRU=2、rmvpe=1、fcpe=1、synthesizer=2；**仅按容量淘汰，无文件 mtime 校验**（同名 pth 被外部覆盖仍返回旧权重） | `cache.py:14-22,84` | `[S]` | ▲ 无护栏 | 已知且被接受的现状 |
| 3.39 | hubert 缓存 key = `(device, is_half, hubert_variant)`；rmvpe key = `(device, is_half)`；fcpe key = `device`；synthesizer key = `pth_path` | `sessions.py:61`、`extractor.py:280,287`、`loader.py:37` | `[S]` | ▲ 无护栏 | |
| 3.40 | `ModelSessions.load` 对同一 `pth_path` 重复调用直接返回缓存 session；load 失败包装为 `ModelLoadError`；成功后对 synthesizer 尝试 `remove_weight_norm()`（失败仅 warning）并 `purge_cuda_graphs(synthesizer, hubert)` | `sessions.py:50-94` | `[S]` | ▲ 无护栏 | |
| 3.41 | `ModelSessions.clear_all`：清 F0 提取器图缓存 → purge LRU 中全部 synthesizer + 当前 session（synthesizer+hubert）→ `_current_pth=None`、`_current_session=None` | `sessions.py:96-120` | `[S]` | ▲ 无护栏 | |
| 3.42 | RMVPE 提取器：`MelSpectrogram(is_half, 128, 16000, 1024, 160, None, 30, 8000)`；`cents_mapping = 20*arange(360)+1997.3794084376191` 两端 pad 4；`local_offsets = arange(9)`；解码在 **fp32** 下进行（`salience.float()`） | `rmvpe/model.py:22-32,125` | `[对拍]` | ▲ 无护栏 | |
| 3.43 | RMVPE 解码：`center=argmax(dim=1)`；`padded=pad(salience,(4,4))`；`index=center+local_offsets`；`divided=Σ(local_salience*local_cents)/Σ(local_salience)`；`cents = where(maxx>thred, divided, 0)`；`f0 = 10*2^(cents/1200)`，`cents>0` 处保留否则 0；带 confidence 时额外返回 `maxx`（**阈值前**原始峰，不被 thred 二值化） | `rmvpe/model.py:54-72,112-140` | `[对拍]` | ▲ 无护栏 | |
| 3.44 | F0 提取器工厂：`method=="rmvpe"` 取 `RMVPE_PATH`（缺失抛 `F0ExtractionError`，文案含中文指引），`"fcpe"` 用 `torchfcpe.spawn_bundled_infer_model`；其他值抛 `F0ExtractionError("未知的 F0 提取方法: ...")`；**工厂内不清 CUDA Graph** | `extractor.py:168-196,272-294` | `[S]` | ▲ 无护栏 | |
| 3.45 | F0 阈值默认值：`rmvpe_threshold=0.05`、`fcpe_confidence_threshold=0.05`（`F0Params` 字段默认） | `config.py:29-31` | `[S]` | ▲ 无护栏 | 训练侧 RMVPE 阈值是 0.03（见 5.9） |
| 3.46 | FCPE 在启用 CUDA Graph 时走自建 `graphable_infer`（local_argmax 局部加权 + `pow(2, decoded/1200)*10`），graph key 含 `fcpe_confidence_threshold`；未启用图时调 `model.infer(..., decoder_mode="local_argmax", threshold=...)` 且 `conf=None` | `extractor.py:221-261` | `[对拍]` | ▲ 无护栏 | |
| 3.47 | 第三方 FCPE 噪声抑制：`_FilteredStream` 重定向 sys.stdout/stderr，按「行首前缀」或「行内包含」丢弃整行；前缀集含 `"[INF0]"`（原文如此）、`"[INFO]"`、`"[WARN]"`、`"[error with torchfcpe.mel_extractor.MelModule]"`、`"min value is "`、`"max value is "`；包含集含 `"torchfcpe.mel_tools.nv_mel_extractor"`、`"Librosa not found"` | `extractor.py:25-75` | `[S]` | ▲ 无护栏 | ⚠ `"[INF0]"` 疑似 typo，但为现状行为，重写保持一致 |
| 3.48 | 缓存/long tensor 复用：`cached_long_tensor(cache, value, device)` 按 int 值缓存 `tensor([value], dtype=long)`；`p_len` 与 `sid` 均经此取得 | `synthesis.py:8-14`、`pipeline.py:298-299` | `[S]` | ▲ 无护栏 | |

## 4. 音频 IO 与 DSP

| # | 行为断言 | 证据(文件:行) | 验证方式 | 护栏 | 备注 |
|---|---|---|---|---|---|
| 4.1 | ffmpeg 解析单源：`resolve_ffmpeg(ffmpeg_exe=None)` 缺省取 `runtime.paths.FFMPEG_EXE`（`assets/ffmpeg/ffmpeg.exe`）；显式传参是唯一覆盖方式（**无进程内改写模块全局的通道**）；路径不存在抛 `FileNotFoundError`（文案含 `assets/ffmpeg/` 指引） | `audio_file.py:22-38`、`paths.py:37` | `[S]`+`[D]` | ▲ 无护栏 | 与「解码失败」刻意区分异常类型 |
| 4.2 | `load_audio(path, target_sr, mono=True, timeout=300, ffmpeg_exe=None)` 命令固定：`[ffmpeg, "-i", path, "-vn", "-acodec","pcm_f32le","-f","f32le","-ac", "1"|"2", "-ar", str(target_sr), "-"]`；`capture_output=True` | `audio_file.py:58-64` | `[S]`+`[对拍]` | ▲ 无护栏 | |
| 4.3 | `load_audio` 异常契约：ffmpeg 非零退出 → `AudioLoadError`（含 stderr 解码后**末 500 字符**）；超时 → `subprocess.TimeoutExpired` 原样抛出；返回 `(float32 ndarray, target_sr)`；`mono=False` 时把交织流 `reshape(-1,2).T` 成 `(2,N)` | `audio_file.py:64-71` | `[S]` | ▲ 无护栏 | |
| 4.4 | `write_wav`：`data.ndim==1` 先 reshape 成 `(N,1)`；`path.parent.mkdir(parents=True, exist_ok=True)`；`FLOAT`→`audio_format=3,bits=32`；`PCM_16`→`audio_format=1,bits=16` 且 `clip(data,-1,1)*32767` astype int16（**越界静默钳制不报错**）；其他 subtype 抛 `ValueError` | `wav_file.py:20-72` | `[对拍]`+`[D]` | ▲ 无护栏 | |
| 4.5 | `write_wav` 输出布局固定：`RIFF` + `fmt `(16) + `data` 三段，`riff_size=36+data_size`；**不写 fact chunk**；字节序小端 | `wav_file.py:57-72` | `[对拍]` | ▲ 无护栏 | 可由确定性输入逐字节 diff |
| 4.6 | `read_wav_info` 返回 `{samplerate, channels, frames, duration, bits_per_sample}`；非 RIFF/WAVE 或缺 fmt/data 抛**裸 `ValueError`**（刻意不进 RVCError 体系，GUI 工具页按 ValueError 捕获）；文件不存在抛 OSError | `wav_file.py:75-122` | `[S]` | ▲ 无护栏 | ⚠ 异常类型属对外行为，见 §10 |
| 4.7 | `read_audio_info(path, ffmpeg_path=None)`：`.wav` 走 `read_wav_info` 只保留 4 键；非 WAV 用 `[ffmpeg, "-hide_banner","-i",path,"-f","null","-"]`（timeout=15）正则解析 stderr；找不到 `Duration:` 抛 `ValueError`；找不到 Stream 行时**返回 `samplerate=0, channels=0, frames=0`（半失败不报错）** | `wav_file.py:125-181` | `[S]`+`[D]` | ▲ 无护栏 | ⚠ 半失败态是现状行为 |
| 4.8 | 非 WAV 声道名映射：`{"mono":1,"stereo":2,"2.1":3,"quad":4,"5.1":6,"7.1":8}`，未知名回退 **2** | `wav_file.py:168-169` | `[S]` | ▲ 无护栏 | |
| 4.9 | `get_audio_devices(hostapi_name=None)` 返回五元组 `(hostapi 名列表, 输入名, 输出名, 输入索引, 输出索引)`；`hostapi_name` 不在可用列表时**回退到第一个 hostapi**（`ha_names[0]`）；设备列表为空时用私有 API `sd._terminate()/_initialize()` 重扫；每个 device dict 注入 `hostapi_name` | `devices.py:5-43` | `[M]` | ▲ 无护栏 | ⚠ 依赖 sounddevice 私有 API |
| 4.10 | mel 滤波器 `mel_filter_bank(sr, n_fft, n_mels=128, fmin=0.0, fmax=None, htk=False, norm="slaney")`：纯 numpy；返回 `(n_mels, 1+n_fft//2)` float32；`fmax=None`→`sr/2`；`norm="slaney"` 用 `enorm=2/(mel_f[2:n+2]-mel_f[:n])`；`norm` 非法（非 None/非法值）抛 `ValueError` | `mel.py:38-73` | `[对拍]` | ▲ 无护栏 | |
| 4.11 | `pad_center(data, size)`：左 pad `int((size-n)/2)`，右 pad 余量，constant 模式 | `mel.py:76-81` | `[对拍]` | ▲ 无护栏 | |
| 4.12 | `hz_to_midi(freq, xp=None)` = `12*log2(freq/440)+69`；`midi_to_hz` = `440*2^((midi-69)/12)`；参考常量 `MIDI_REF_FREQ=440.0`、`MIDI_REF_NOTE=69`；`xp` 缺省按输入是否 tensor 推断（numpy/torch 双后端） | `hz_midi.py:9-29` | `[对拍]` | ▲ 无护栏 | |
| 4.13 | 跨层共享常量：`HUBERT_SAMPLE_RATE=16000`、`HUBERT_FRAME_SIZE=160`、`RMVPE_THRESHOLD_TRAIN=0.03`（训练侧；推理侧默认 0.05 是 `F0Params` 字段，刻意不同） | `constants.py:3-10`、`config.py:30` | `[S]` | ▲ 无护栏 | |
| 4.14 | `nan_to_num`/钳制策略固定：F0 后处理 NaN/inf → 0；RMS 的 `r2` 钳下限 1e-3；`fast_rms` 输出钳 1e-8；SOLA 相关分母 `+1e-8` | `postprocess.py:125-127`、`rms.py:36,68`、`sola.py:55` | `[对拍]` | ▲ 无护栏 | |

## 5. 训练管线

| # | 行为断言 | 证据(文件:行) | 验证方式 | 护栏 | 备注 |
|---|---|---|---|---|---|
| 5.1 | `Slicer(sr, threshold=-42.0, min_length=1500, min_interval=400, hop_size=15, max_sil_kept=500)`：阈值转线性 `10^(dB/20)`，其余按毫秒×sr/1000 转样本；`wav.shape[0] <= min_length` 时**整段返回**（不切片） | `preprocess.py:45-56` | `[对拍]` | ▲ 无护栏 | |
| 5.2 | `_rms`（训练切片用）：reflect pad `frame_length//2`，用 `as_strided` 分帧，`sqrt(mean(frame^2))`（float64 计算，输出 float32），`frame_length=hop*2, hop=hop` | `preprocess.py:29-42` | `[对拍]` | ▲ 无护栏 | |
| 5.3 | 切片算法：以 RMS<threshold 判静音；静音段 ≥`min_interval` 且 `silence_end-clip_start>=min_length` 时在 `silence_start + min(silence_len//2, max_sil_kept)` 处落切点；末尾追加 `(clip_start, len(wav))`；仅保留 `end-start > hop_size` 的段 | `preprocess.py:57-84` | `[对拍]` | ▲ 无护栏 | |
| 5.4 | `PreProcessor(input_dir, exp_dir, sr, per=3.7, ffmpeg_exe=None)`：`signal.butter(5, 48, btype="high", fs=sr)` 高通；`rglob` 收集扩展名 ∈ `{.wav,.mp3,.flac,.ogg,.m4a,.aac,.wma,.opus}` 的文件；`run()` 返回**源文件数**（非切片数） | `preprocess.py:87-108,14` | `[对拍]` | ▲ 无护栏 | `_AUDIO_EXTS` 与 `autodl.AUDIO_EXTS` 集合一致 |
| 5.5 | 每文件：解码 → 高通滤波 → 切片 → 按 `per` 秒切块、块间重叠 `int(0.3*sr)`；`len(chunk) < sr` 的尾块**丢弃**；`normalize_audio` 后写 `0_gt_wavs/{i}_{idx}.wav`（FLOAT，sr）与 `1_16k_wavs/同名`（FLOAT，16000，`signal.resample` 到 `int(len*16000/sr)`，仅当 sr!=16000） | `preprocess.py:122-145` | `[对拍]`+`[D]` | ▲ 无护栏 | 文件名 `{file_index}_{idx}.wav`，`file_index` 1 基 |
| 5.6 | `normalize_audio`：`peak=abs(wav).max()`；`peak>2.5` → 返回**空数组**；`peak>0` 时 `wav = wav/peak*0.9*0.75 + wav*0.25`；输出 float32 | `preprocess.py:148-155` | `[对拍]` | ▲ 无护栏 | |
| 5.7 | `build_exp_manifest(input_dir, sr, per)` 指纹：对 `rglob` 下音频文件按 `相对路径(\\→/)|size|int(mtime)` 排序后 join，**sha1** 为 `input_hash`；返回 `{input_dir(已 resolve), input_hash, sr, per}` | `preprocess.py:158-171` | `[D]` | — | manifest.json 字节可 diff |
| 5.8 | `_prepare_exp_dir`：指纹（仅 `input_dir/input_hash/sr/per` 四键）与旧 manifest 不同则 `clear_exp_runtime` 后再写新 manifest；`clear_exp_runtime` 删 `0_gt_wavs/1_16k_wavs/2a_f0/2b-f0nsf/3_feature768/spec_cache/filelist.txt` **及** 根目录与 `4_checkpoints/` 下 `G_*.pth`/`D_*.pth` | `preprocess.py:110-120,16-25,174-190` | `[D]` | — | |
| 5.9 | `manifest_diff_reason` 检查顺序固定：manifest 缺失 → `input_dir` → `sr` → `per` → `input_hash`；一致返回 `""`；`manifest_matches` 为其布尔取反 | `preprocess.py:193-212` | `[D]` | — | 文案固定（GUI/autodl 直接展示） |
| 5.10 | `generate_filelist(exp_dir)`：按 `0_gt_wavs/*.wav` 字典序，要求 `3_feature768/{stem}.npy`、`2a_f0/`、`2b-f0nsf/` 三件齐备，写 `<exp>/filelist.txt` 行格式 `wav|feat|f0|f0nsf|0`，返回 `(filelist_path, 行数)` | `preprocess.py:215-231` | `[D]` | — | 第 5 列 sid 恒 `0` |
| 5.11 | `TrainF0Extractor(device="cuda:0", is_half=True, ffmpeg_exe=None)`：`run()` 遍历 `1_16k_wavs/*.wav` **字典序**，产物已存在则跳过；`RMVPE.infer_from_audio(wav, thred=0.03)`；`2b-f0nsf/{stem}.npy` 存 float32 连续 F0，`2a_f0/{stem}.npy` 存 `normalize_f0_to_coarse(f0)`；返回**文件数**（非实际处理数） | `extract_f0.py:14-53` | `[对拍]`+`[D]` | ▲ 无护栏 | 阈值 0.03 与推理 0.05 刻意不同 |
| 5.12 | `HuBERTExtractor(device="cuda:0", is_half=True, hubert="base", ffmpeg_exe=None)`：进程级 `_hubert_cache = LRUCache(2)`，key `(device,is_half,hubert)`；`run()` 遍历 `1_16k_wavs`，`3_feature768/{stem}.npy` 存在则跳过；`extract()` 用 `model(feats).last_hidden_state.squeeze(0).float().cpu().numpy()`（float32）；返回**文件数** | `extract_feature.py:20-67` | `[对拍]`+`[D]` | ▲ 无护栏 | 构造函数默认 `"base"`（与 `HUBERT_DEFAULT="chinese"` 不同，见 §11） |
| 5.13 | 训练 JSON 单源：`load_train_json(sr)` 经 `train_config_path(sr)`——`parse_sr` 支持 `"40k"/"48000"`，未支持/缺省回退 `DEFAULT_TRAIN_SR=48000`；目标文件不存在抛 `FileNotFoundError` | `checkpoint.py:104-106`、`paths.py:18-22,50-70` | `[S]`+`[D]` | — | |
| 5.14 | `Trainer.__init__`：`segment_size = train_cfg["segment_size"] // data_cfg["hop_length"]`；从 `model` 段 `pop("use_spectral_norm", False)` 并**不**传入生成器；`log_file = <exp_dir>/train.log`；`export_dir` 缺省 `MODELS_DIR`（`assets/models`） | `trainer.py:49-59` | `[S]` | — | use_spectral_norm 只属判别器 |
| 5.15 | `Trainer.setup`：`torch.manual_seed/random.seed/np.random.seed` 用 `train_cfg["seed"]`（JSON 中 1234）；G 用 `SynthesizerTrnMsNSFsid(spec_channels, segment_size, **model_cfg, is_half=fp16, sr=sr)`，D 用 `MultiPeriodDiscriminatorV2(use_spectral_norm)`；两优化器均 `AdamW(lr, betas=train_cfg["betas"], eps=train_cfg["eps"])` | `trainer.py:81-95`、`assets/configs/48ktrain_config.json:4` | `[S]` | — | |
| 5.16 | 续训：`4_checkpoints/` 下 `G_*.pth` 与 `D_*.pth` 都存在时加载两者，`start_epoch = min(epoch_g, epoch_d) + 1`，log `恢复训练: epoch N（G=... D=...）`；否则按 `pretrain_g`/`pretrain_d` 非空加载底模（`strict=False`），log `加载预训练 G/D` | `trainer.py:96-114` | `[D]`+`[S]` | — | `latest_checkpoint_path` 以 `_(\d+)\.pth$` 取最大 epoch |
| 5.17 | 调度器：`ExponentialLR(gamma=train_cfg["lr_decay"], last_epoch=start_epoch-2)`；`GradScaler("cuda", enabled=fp16_run)`；DataLoader 用 `BucketSampler` + `num_workers=2` + `pin_memory=True`；`len(loader)==0` 抛 `RuntimeError("训练样本不足，无法组成 batch")` | `trainer.py:116-129` | `[S]` | — | |
| 5.18 | 训练循环每 epoch：`_train_epoch` → `scheduler_g.step()`+`scheduler_d.step()` → 追加写 `train.log` 一行 `epoch {epoch:4d} | D {:.4f} | G {:.4f} | Mel {:.4f} | KL {:.4f} | FM {:.4f}`；`epoch % save_every_epoch == 0` 或 `epoch == epochs` 或 `stop_requested` 时 `_save(epoch)`；`progress_callback(epoch, epochs)` | `trainer.py:131-159` | `[D]` | — | 行格式与分隔符固定 |
| 5.19 | 单 batch 顺序：AMP autocast 下 `synthesizer(...)` → `spec_to_mel_torch(spec,...)` → `slice_segments(mel, ids_slice, segment_size)` → `mel_spectrogram_torch(y_hat...)` → `slice_segments(wave, ids_slice*hop, train_cfg["segment_size"])` → D 前向（detach）→ `discriminator_loss`；`optim_d` 反传后再 G 前向算 `loss_mel*loss_c_mel`、`loss_kl*loss_c_kl`、`feature_loss`、`generator_loss`，`loss_gen_all = gen+fm+mel+kl`；两优化器均 `zero_grad(set_to_none=True)`、`scaler.unscale_`、`step`，`scaler.update()` 每 batch 一次 | `trainer.py:168-199` | `[对拍]` | ▲ 无护栏 | 数值路径与上游等价，禁止化简 |
| 5.20 | `loss_callback` 每 batch 上报 dict，键固定：`epoch/batch/loss_d/loss_g/loss_total/loss_mel/loss_kl/loss_fm`；其中 `loss_g` = **纯对抗 `loss_gen`**（不含 FM/Mel/KL），`loss_total` = `loss_gen_all`；`batch_callback(epoch, batch_idx, total_batches)` | `trainer.py:201-218` | `[S]` | — | 口径与上游 `train.py` loss_gen 对齐 |
| 5.21 | CUDA OOM 被翻译为中文 `RuntimeError`（提示调小 batch / segment），`from None` 抑制原异常链；训练在首 epoch 前被停止（`last_epoch is None`）抛 `RuntimeError("训练在首个 epoch 前已停止")` | `trainer.py:139-144,156-157` | `[S]` | — | |
| 5.22 | `_save(epoch)`：写 `4_checkpoints/G_{epoch}.pth`、`D_{epoch}.pth`（`save_checkpoint`：`{model, iteration, optimizer, learning_rate}`，`.tmp`+`os.replace` 原子写）；按 `keep_ckpts=max(...,1)` 分别对 `G_*.pth`/`D_*.pth` 保留最新 N；导出 `<export_dir>/{exp_name}_e{epoch}.pth`；`keep_models>0` 时按 `exported_epoch` 只淘汰本实验 `<exp>_e*.pth` | `trainer.py:230-257`、`checkpoint.py:17-29,86-101` | `[D]` | — | 产物文件清单可 diff |
| 5.23 | `export_model`：剔除所有 `enc_q` 前缀键；其余权重 `detach().cpu().half()`；写 `{weight, config(18 元元组, 见 5.24), info, sr:"{sr//1000}k", f0:1, version:"v2"}`；`info = re.sub(r"_e\d+$","",stem)`；`.tmp`+`os.replace` | `checkpoint.py:109-128` | `[D]` | — | 与官方模型互操作的关键 |
| 5.24 | `build_model_config` 18 元顺序固定：`[filter_length//2+1, 32, inter_channels, hidden_channels, filter_channels, n_heads, n_layers, kernel_size, p_dropout, resblock, resblock_kernel_sizes, resblock_dilation_sizes, upsample_rates, upsample_initial_channel, upsample_kernel_sizes, spk_embed_dim, gin_channels, sr]` | `checkpoint.py:235-257` | `[S]` | — | 顺序错即不兼容旧模型 |
| 5.25 | `load_checkpoint` 宽松加载：逐键比对存在且形状一致才 update，其余进 `skipped`；`skipped` 非空时 `logger.warning(... 有 N 个权重未匹配被跳过: 前 8 个)`；optimizer 非 None 时恢复其 state；返回 `(learning_rate, iteration)` | `checkpoint.py:32-55` | `[S]` | — | |
| 5.26 | `merge_models(a, b, ratio, out)`：两者 `config` 不等抛 `ValueError("两个模型架构不一致（config 不同），无法合并")`；共有键 `a*ratio + b*(1-ratio)`（float 计算后 half），a 独有键直接取 a，b 独有键补入；输出 `{weight, config, info=stem, sr, f0, version}`；`.tmp`+`os.replace` | `checkpoint.py:131-157` | `[D]` | — | |
| 5.27 | `inspect_model(path)` 输出行（顺序固定）：`真名/模型信息: {zip原名 或 info 或 stem}`、`Info: {info}`（非空才出）、`文件大小: {:.1f} MB`、`采样率: {sr}`、`版本: {version}`、`F0 支持: 是/否` | `checkpoint.py:168-185` | `[D]` | — | GUI 依赖首行含 `真名/模型信息:` 做解析（`tools_inspect.py:72-74`）。⚠ **已被 §12 取代**（D-3 唯一批准变更：首行改优先 info、zip 原名独立成行） |
| 5.28 | `change_archive_name(path, new_name)`：去掉尾 `.pth`（大小写不敏感）；空 → `ValueError("新原名不能为空")`；非 zip 模型 → `ValueError("该文件不是 zip 打包的模型，无法修改原名")`；同名直接返回；否则逐条重写 zip 内 `{old}/` 前缀并保留 `date_time/compress_type/external_attr/internal_attr`；返回新名 | `checkpoint.py:160-165,188-221` | `[D]` | — | |
| 5.29 | `change_info(path, info)`：`info` 去尾 `.pth` 后写回 ckpt；若原文件是 zip 打包则用 `_rewrite_archive_prefix(tmp, archive)` 复原其 zip 原名；返回 `None` | `checkpoint.py:224-232` | `[D]` | — | |
| 5.30 | `TextAudioLoaderMultiNSFsid` 帧长过滤：`length = np.load(feat).shape[0] * 2`，仅保留 `100 <= length <= 900`；filelist 行必须正好 5 列且四路径都存在才入列；`_filter` 同时产出 `self.lengths` | `dataset.py:44-63` | `[S]`+`[D]` | — | |
| 5.31 | `__getitem__`：`phone = load(feat).repeat_interleave(2, dim=0)`；`pitch` int64；`pitchf` float32；`spec` 与 `wav` 来自 `_get_audio`；四者取 `min_len = min(phone,pitch,pitchf,spec)` 统一截断；`max-min > 4` 时 `logger.warning("特征长度不对齐 ...")`；返回 `(phone,pitch,pitchf,spec,wav,int(sid))` | `dataset.py:65-83` | `[S]` | — | ≤4 帧差异静默截断（正常） |
| 5.32 | `_get_audio` STFT 缓存：文件名 `{stem}.sr{sr}.n{n_fft}.h{hop}.w{win}.spec.pt`，落在 `{exp}/spec_cache/`；有效判据 `spec 存在 且 spec.mtime >= wav.mtime`；未命中则 `spectrogram_torch(..., center=False)` 后 `torch.save`；`load_audio` 采样率与 data_cfg 不符抛 `ValueError` | `dataset.py:88-118` | `[D]` | — | ⚠ A6：缓存**不再**写进 `0_gt_wavs/` |
| 5.33 | `TextAudioCollateMultiNSFsid`：按 `x[3].size(1)`（spec 长度）降序排序；输出 9 元 `(phone, phone_lengths, pitch, pitchf, spec, spec_lengths, wav, wav_lengths, sid)`，其中 `pitch` long、`pitchf`/`phone`/`spec`/`wav` float32 零填充 | `dataset.py:121-154` | `[对拍]` | ▲ 无护栏 | |
| 5.34 | `BucketSampler(dataset, batch_size, boundaries=None, shuffle=True)`：默认 `boundaries=[100,...,900]`；`_bisect` 用 `bisect_right-1`，越界返回 -1 并计入 `dropped`（warning `BucketSampler：%d/%d 样本被跳过（长度超出范围 [100, 900]）`）；`__iter__` 桶内 shuffle、每桶整除 batch 分组、再整体 shuffle；`__len__` = Σ(桶长//batch_size) | `dataset.py:157-197` | `[D]` | — | |
| 5.35 | 损失函数数值口径固定（禁止化简）：`feature_loss` 返回 `ΣΣ mean(abs(detach(r)-g)) * 2`；`discriminator_loss` 每层 `mean((1-dr)^2)+mean(dg^2)` 求和，返回 `(loss, real_losses, generated_losses)`；`generator_loss` 每层 `mean((1-dg)^2)` 求和，返回 `(loss, generated_losses)`；`kl_loss = (logs_p - logs_q - 0.5 + 0.5*(z_p-m_p)^2*exp(-2*logs_p))` 乘 mask 求和后除以 mask 和 | `losses.py:9-52` | `[对拍]` | ▲ 无护栏 | |
| 5.36 | `spectrogram_torch`：reflect pad `(n_fft-hop)//2` 两侧；half 输入转 float 再 STFT；返回 `sqrt(real^2+imag^2+1e-6)`；hann 窗按 `{win}_{dtype}_{device}` 缓存 | `mel_processing.py:31-55` | `[对拍]` | ▲ 无护栏 | |
| 5.37 | `spec_to_mel_torch`：`mel_filter_bank(htk 未开)` → matmul 后 `log(clamp(x, 1e-5))`；mel basis 按 `{sr}_{n_fft}_{n_mels}_{fmin}_{fmax}_{dtype}_{device}` 缓存；`MAX_WAV_VALUE = 32768.0` | `mel_processing.py:9,58-64` | `[对拍]` | ▲ 无护栏 | |
| 5.38 | 训练配置 JSON 内容固定（40k/48k 差异仅在 `sr/hop/n_mel_channels/upsample_rates/upsample_kernel_sizes/segment_size`）：`train.seed=1234`、`lr_decay=0.999875`、`c_mel=45`、`c_kl=1.0`、`betas=[0.8,0.99]`、`model.spk_embed_dim=109`、`gin_channels=256`；`log_interval=200` 字段**存在但无任何代码引用**（死字段） | `assets/configs/40ktrain_config.json`、`assets/configs/48ktrain_config.json` | `[S]`+`[D]` | ▲ 无护栏 | 两侧必须保持等价语义（重写可换文件格式但键值语义不变） |
| 5.39 | `TrainConfig` dataclass 字段与默认值：`exp_dir`(必填)、`sr=48000`、`epochs=2000`、`batch_size=4`、`save_every_epoch=200`、`learning_rate=1e-4`、`pretrain_g=""`、`pretrain_d=""`、`fp16_run=True`、`device="cuda:0"`、`keep_ckpts=1`、`keep_models=0` | `config.py:93-107` | `[S]` | — | 与 GUI/autodl 各自的展示默认不同（见 §11） |
| 5.40 | `Trainer.cleanup()` 把 `synthesizer/net_d/optim_g/optim_d/scheduler_g/scheduler_d` 置 None 并 `torch.cuda.empty_cache()` | `trainer.py:64-69` | `[S]` | — | 由调用方在 worker 线程 finally 中调用 |

## 6. 推理 GUI

| # | 行为断言 | 证据(文件:行) | 验证方式 | 护栏 | 备注 |
|---|---|---|---|---|---|
| 6.1 | 主窗口 `MainWindow(WindowLifecycle, QMainWindow)`：标题 `"RVC"`、`resize(200, 150)` | `window.py:60-61` | `[S]`+`[M]` | ▲ 无护栏 | |
| 6.2 | 页签结构固定 4 个、名称与顺序固定：`设备驱动`（`build_audio_driver_tab`）、`音色调节`（`build_timbre_tab`）、`性能调节`（`build_performance_tab`）、`离线推理`（`build_offline_tab`） | `window.py:120-128` | `[S]` | — | 旧名「参数调节/高级功能」已改名 |
| 6.3 | 底部控制栏：左侧单个 `btn_toggle`（初值文案 `"开始"`、`primary` 样式、尺寸 `Layout.BTN_WIDTH_NORMAL × BTN_HEIGHT_NORMAL` = 60×28）、`pitch_lbl`(`"音高: -"`, minWidth 100)、`delay_lbl`(`"延迟: -"`, minWidth 120)、`error_lbl`(`""`, minWidth 70, `Colors.ERROR` 色, **初始 hidden**) | `window.py:131-164`、`layout.py:18-22` | `[S]` | — | |
| 6.4 | 参数绑定表 `BINDINGS` 固定 **13** 项（分组点路径 + 控件名 + 读写方式）：`voice.formant/formant_slider/FLOAT`、`rms_mix/rms_mix_slider/FLOAT`、`f0.method/f0_rmvp_btn/RADIO_F0`、`buffer.block_time/block_time_slider/FLOAT`、`buffer.crossfade_time/crossfade_slider/FLOAT`、`buffer.extra_time/extra_time_slider/FLOAT`、`audio.sr_mode/sr_model_radio/RADIO_SR`、`audio.hostapi/hostapi_combo/COMBO`、`audio.input_device/input_combo/COMBO`、`audio.output_device/output_combo/COMBO`、`audio.output2_device/output2_combo/COMBO`、`model_path/model_path/ATTR`、`hubert/hubert_combo/COMBO` | `bindings.py:26-47` | `[S]` | — | 旧文档「14 项」有误 |
| 6.5 | 特殊控件（不在 BINDINGS）：`f0_threshold_slider` 按当前 `f0.method` 写 `rmvpe_threshold` 或 `fcpe_confidence_threshold`；`pitch_map_src_range`/`pitch_map_dst_range` 的 `rangeChanged` 写 `voice.pitch_map_*` | `window.py:203-225`、`bindings.py:173-188,210-221` | `[S]` | — | |
| 6.6 | 信号只连接一次（`_signals_connected` 幂等标志）；`DoubleSlider` 的 `valueChanged` 必须读 `slider.value()` 取物理值（信号参数是内部编码整数） | `window.py:175-193,217-225`、`widgets.py:29-41` | `[S]` | — | |
| 6.7 | `DoubleSlider`：内部整数编码 `int = round(物理值/step)`，`value()` 返回 `int*step`，`setValue(v)` 用 `round(v/step)`；`minimum()/maximum()` 返回物理值 | `widgets.py:9-41` | `[S]` | — | |
| 6.8 | `RangeSlider`：内部直接存 float；`setRange` 钳到 `[min,max]`、low>high 时交换、必定 `rangeChanged.emit(low,high)`；拖动步进 `round(value/step)*step`；`low` 不允许 ≥ `high`（保 `_step` 间隔） | `widgets.py:94-222` | `[S]` | — | |
| 6.9 | 滑条/范围控件 label 挂载命名约定：`_create_slider_row` 用 `attr[:-7]+"_label"`（去 `_slider`），`_range_row` 用 `attr[:-6]+"_label"`（去 `_range`） | `widgets.py:70`、`global_params_tab.py:27` | `[S]` | — | |
| 6.10 | 持久化文件 `assets/configs/save_state.json`，结构 `{"gui": {...}, "train": {...}}`，`json.dumps(ensure_ascii=False, indent=2)` UTF-8；写前 `mkdir(parents=True, exist_ok=True)`；`_lock` 互斥；`.tmp` + `os.replace` 原子写 | `configs/config.py:33-41`、`paths.py:14-15` | `[D]` | — | |
| 6.11 | 配置读取：文件不存在或内容异常 → 返回 `{"gui": {}, "train": {}}`；JSON 损坏时把原文件 `os.replace` 成 `<name>.json.bak` 后返回默认并 `logger.warning`（备份失败仅 warning 不抛） | `configs/config.py:12-30` | `[D]` | — | ⚠ 无旧 `"experimental"` 键迁移逻辑（已删） |
| 6.12 | `params_to_dict` 序列化分组键固定：`voice{formant,pitch_map_src_min,pitch_map_src_max,pitch_map_dst_min,pitch_map_dst_max}`、`f0{method,rmvpe_threshold,fcpe_confidence_threshold}`、`buffer{block_time,crossfade_time,extra_time}`、`audio{sr_mode,hostapi,input_device,output_device,output2_device,enable_out2}`、顶层 `rms_mix/model_path/hubert` | `bindings.py:105-136` | `[D]` | — | |
| 6.13 | `params_from_dict` 缺键回退到 `InferenceParams()` 实例默认（单源 `_DEFAULTS`），不写字面量；`audio.enable_out2` **不从 dict 读**，由 `output2_device` 重算 | `bindings.py:17,64-102` | `[D]` | — | 往返幂等 |
| 6.14 | 保存时机固定：点「开始」前（`_start`，失败仅 warning）、离线转换前（`_off_start`）、模型加载成功回调 `_on_loaded` 内、托盘退出（`_tray_quit`，失败 `logger.error`） | `lifecycle.py:142-147,256-259,204,65-68` | `[M]` | ▲ 无护栏 | |
| 6.15 | 遥测：`QTimer` 200ms 轮询 `controller.snapshot()`；`running and measure_ms>0` 时更新 `延迟: {ms:.0f}ms`；`input_pitch>0` 时 `音高: {pitch:.0f}Hz` 否则 `音高: -`；`error_count>0` 显示 `错误: {n}` 否则隐藏 | `lifecycle.py:68,295,101-118` | `[M]`+`[S]` | ▲ 无护栏 | 无浊音帧不刷新音高标签 |
| 6.16 | `RuntimeTelemetry` 为 `frozen=True` dataclass，字段 `running/measure_ms/input_pitch/error_count=0`；`InferController.snapshot()` 在引擎未构造或未运行时返回 `RuntimeTelemetry(running=False, measure_ms=0.0, input_pitch=0.0)`（**不触发惰性构造**） | `telemetry.py:9-15`、`engine_controller.py:73-83` | `[S]` | — | |
| 6.17 | 引擎惰性构造：`InferController.engine` 双检锁（`_engine_lock`）内 `from rvc.streaming.engine import VoiceEngine` 并注入 `runtime_params`/`inference_cache`/`on_runtime_error` | `engine_controller.py:39-49` | `[S]` | — | 避免预热线程与主线程造双实例 |
| 6.18 | 窗口构建后用 `QTimer.singleShot(300, self._warmup_engine)` 在 daemon `threading.Thread(name="engine-warmup")` 中预热；预热失败 `logger.warning` 并 `QTimer.singleShot(0, ...)` 回主线程 `_show_error`（文案含「请确认已安装 NVIDIA 显卡驱动并支持 CUDA」） | `window.py:81`、`lifecycle.py:83-97` | `[M]` | ▲ 无护栏 | |
| 6.19 | 托盘：`TrayManager` 在系统托盘不可用时抛 `RuntimeError`，`MainWindow` 捕获后 `QMessageBox.critical` + `sys.exit(1)`（**不降级**）；图标经 `_load_or_make_icon`：文件存在则加载，否则程序绘制（64×64 圆底+三白杠）并尝试保存到 `assets/resources/icon_{idle,active}.png`（保存失败仅 warning） | `window.py:85-93`、`tray.py:36-58,17-33` | `[M]` | ▲ 无护栏 | idle 红 `(211,47,47)`、active 绿 `(46,125,50)` |
| 6.20 | 托盘菜单固定 `显示` + 分隔线 + `退出`；单击（`Trigger`）切换开始/停止（不打开窗口）；`update_status` 用 `measure_ms` 设 tooltip（`-` 或 `{ms:.0f}ms`）并切图标底色 | `tray.py:68-104` | `[M]` | ▲ 无护栏 | |
| 6.21 | 关窗行为：`closeEvent` **`event.ignore()` + `hide()`**（不退出），首次隐藏触发气泡 `已最小化到托盘，点击图标恢复窗口`（延时 300ms、2500ms 寿命，仅一次） | `lifecycle.py:74-79`、`tray.py:106-119` | `[M]` | ▲ 无护栏 | |
| 6.22 | 托盘退出顺序固定：停 timer → 停 `_lt` 加载线程（`quit()`+`wait(2000)`）→ `_save_gui_config()` → `controller.stop()` → `QApplication.instance().quit()` | `lifecycle.py:57-72` | `[M]` | ▲ 无护栏 | |
| 6.23 | 启动流程：`_start` 校验 `model_path` 非空（否则 `_show_warning("请先在参数调节中选择模型文件")`）→ 读 `hubert_combo` → `_apply_runtime_params()`（`update_from(collect_gui_state())` 原地更新）→ 存配置 → `_start_engine` | `lifecycle.py:131-149`、`window.py:266-274` | `[S]` | ▲ 无护栏 | 警告文案固定（含旧词「参数调节」） |
| 6.24 | 模型加载线程：`LoadThread(QThread)` 带 `ok(int)`/`err(str)` 信号，`run()` 内 `engine.load_model(pth, True, hubert)`（**force=True**）；加载期间按钮文案 `"加载中"` 且禁用（secondary 样式），完成后 `_mark_running` → 按钮 `"停止"`（danger 样式）并 `_timer.start(200)` | `widgets.py:75-91`、`lifecycle.py:151-173,290-295` | `[M]`+`[S]` | ▲ 无护栏 | |
| 6.25 | 重复点「开始」加载中：请求停旧线程（`wait(3000)` 超时仅 warning）→ 断开旧信号（吞 RuntimeError/TypeError）→ `deleteLater` → 重新加载 | `lifecycle.py:152-167` | `[M]` | ▲ 无护栏 | |
| 6.26 | 音频回调错误经 `runtime_error = Signal(str)` 转发主线程（回调线程只 emit）；主线程 `_handle_runtime_error` → `controller.handle_runtime_error`（复位 `runtime_error_pending` + `engine.stop()` + `logger.error`）→ `_reset_runtime_ui` → 刷新托盘 → `_show_error(f"实时推理错误: {message}")` | `window.py:55-56,64`、`lifecycle.py:212-222`、`engine_controller.py:133-141` | `[S]` | — | 禁止回调线程操作 Qt |
| 6.27 | `_reset_runtime_ui`：停 timer、按钮回 `"开始"`+primary、清空延迟/音高标签为 `-`、隐藏 error 标签 | `window.py:283-288` | `[S]` | — | |
| 6.28 | 设备刷新：运行中点击「刷新」弹 warning `运行中不能刷新设备，请先停止` 并 return；`DeviceCatalog.reload_devices` 先 `sd._terminate()/_initialize()` 再重填下拉，尽量恢复原选中项（`_select` 找不到则保持首项），重填 hostapi 前 `blockSignals(True)` 防递归 | `window.py:242-247`、`device_controller.py:16-59` | `[M]` | ▲ 无护栏 | |
| 6.29 | 设备下拉 → PortAudio 全局索引映射：输入为 0 基位置（越界抛 `IndexError`）；输出 `combo_idx == -1` 是副输出「不使用」的有意取值，返回 -1，其余越界抛 `IndexError` | `device_controller.py:80-99` | `[S]` | — | 越界**不静默回退** |
| 6.30 | 副输出下拉固定首项 `"不使用"` 再跟全部输出设备；`_on_loaded` 传 `output2_combo.currentIndex() - 1` 给 `get_output_device_index` | `device_controller.py:76-78`、`lifecycle.py:193` | `[S]` | — | |
| 6.31 | `setup_engine` 把 `sr_mode("model"→"sr_model" 否则 "sr_device")` 传给 `engine.setup`；`out2_idx = output2_device_idx if (enable_out2 and output2_device_idx>=0) else None`；启动异常时先 `engine.stop()` 再 re-raise；返回 `EngineStats(sr_model, sr_dev)` | `engine_controller.py:87-105` | `[S]` | — | |
| 6.32 | 模型加载成功后把 radio 文案写成 `模型 {sr}` / `设备 {sr}`，再 `_save_gui_config` | `lifecycle.py:199-204` | `[S]` | — | |
| 6.33 | 离线转换：独立 `VoiceEngine(self.params)`（`OfflineParams`），先 `load_model`（进度 20/100），再 `process_file(progress_cb=_progress)`，逐块进度映射 `pct = 20 + int(cur*80/total)`，结束 `(100,100)` 并 emit `output_path`；`run()` finally 里 `torch.cuda.empty_cache()` | `offline_controller.py:168-184` | `[D]`+`[M]` | ▲ 无护栏 | 进度语义固定 |
| 6.34 | 离线前置校验顺序与文案固定：转换中→`已有转换任务正在运行`；`controller.is_running`→`实时推理正在运行，请先停止实时推理再进行离线转换`；无输入→`请选择输入文件`；文件不存在→`文件不存在: {inp}`；无输出→自动补 `{base}_converted.wav`；无模型→`请先在「音色调节」中选择模型文件` | `offline_controller.py:54-83` | `[D]` | — | `is_running` 经 controller 短路，不触发引擎惰性构造 |
| 6.35 | 离线文件对话框：输入 filter `"音频 (*.wav *.mp3 *.flac *.m4a *.aac *.ogg *.opus)"`、标题 `选择输入文件`；输出 `getSaveFileName` filter `"WAV (*.wav)"`、标题 `保存音频`；选完输入后若输出为空自动填 `{base}_converted.wav` | `offline_controller.py:29-52` | `[S]` | — | |
| 6.36 | 离线 `OfflineWorker` 信号 `progress(int,int)/finished(str)/error(str)`；`worker` 引用保留不 `deleteLater`/`wait`（避免 Qt 销毁竞态警告） | `offline_controller.py:122-139,142-151` | `[S]` | — | |
| 6.37 | 离线转换时按钮禁用+文案 `"转换中..."`、状态 `"初始化..."`、进度归 0；`_on_progress` 状态 `进度: {cur}/{total}`；完成 `"完成"`；错误 `"错误"` + `_show_error(f"离线推理错误: {msg}")` | `offline_controller.py:111-139` | `[D]` | — | |
| 6.38 | View 层不得顶层 `import rvc.*`（仅允许 `TYPE_CHECKING` 与 Controller 转发）；`gui/infer/view/window.py` 仅在 `TYPE_CHECKING` 下 import `InferenceParams` | `window.py:47-49`、`engine_controller.py:62-71`、`tray.py:53` | `[S]` | — | AST 可静态守护 |
| 6.39 | 主题：`apply_theme(app)` 固定 `app.setStyle("Fusion")` + QPalette（Win11 深色）+ 全局 QSS | `theme.py:217-225` | `[S]`+`[M]` | ▲ 无护栏 | 色值常量见 `colors.py` |

## 7. 训练 GUI

| # | 行为断言 | 证据(文件:行) | 验证方式 | 护栏 | 备注 |
|---|---|---|---|---|---|
| 7.1 | 主窗口 `TrainWindow(QMainWindow)`：标题 `"RVC 训练"`、`resize(470, 400)` | `train/view/window.py:20-21` | `[S]`+`[M]` | ▲ 无护栏 | |
| 7.2 | 页签固定 3 个、名称与顺序固定：`设置`（`build_settings_tab`）、`训练`（`build_train_tab`）、`工具`（`build_tools_tab`） | `train/view/window.py:35-39` | `[S]` | — | |
| 7.3 | 设置页签分两组：`数据设置`（实验名 `QLineEdit("exp01")`、音频目录+浏览、采样率 combo `["40k","48k"]`、特征器 combo `["base","chinese"]`）；`训练参数`（Epoch 1..100000、Batch 1..64、保存频率 1..100000、学习率 `QLineEdit("1e-4")`、预训练 G/D + 浏览） | `settings_tab.py:25-100` | `[S]` | — | |
| 7.4 | 采样率切换自动匹配底模：`pretrained_paths(sr_text)` 返回 `<PRETRAINED_ROOT>/f0G{sr}.pth`、`f0D{sr}.pth`，**存在才返回路径否则空串**（从零训练） | `train/view/window.py:45-54`、`train_controller.py:18-29` | `[D]` | — | |
| 7.5 | 训练页签按钮文案固定：`1. 预处理`/`2. 提取F0`/`3. 提取特征`/`4. 训练`/`一键全流程`/`停止训练`（初始 disabled、muted 样式）；`4.训练` 与 `一键全流程` 为 primary | `train_tab.py:20-39` | `[S]` | — | |
| 7.6 | 训练页签进度组：`stage_label`(`当前阶段: 未开始`)、`epoch_label`(`Epoch: -`)、`loss_label`(`Loss: -`)、`progress_bar`(range 0..100)；日志为只读 `QTextEdit`（`on_log` 追加后滚动到底） | `train_tab.py:53-75`、`train/view/window.py:217-219` | `[S]` | — | |
| 7.7 | 工具页签聚合三组（顺序固定）：`模型合并`（`tools_merge`）、`模型信息`（`tools_inspect`）、`修正模型信息（真名）`（`tools_fixinfo`） | `tools_tab.py:12-15` | `[S]` | — | ⚠ 其中「真名」措辞受 §12-③ 消歧影响（界面文案将调整；分组顺序不变） |
| 7.8 | `_start_step(step)` 流程：`validate_options` 失败 → `QMessageBox.warning("参数错误", msg)` 并 return；成功则先 `_save_cfg`（失败静默）→ `_set_running(True)` → `log_edit.clear()` → 建 `TrainWorker(options, step)` 并连 7 信号 → `_batch_t0=None` → `start()` | `train/view/window.py:56-79` | `[S]` | — | |
| 7.9 | `TrainWorker` 信号固定 8 个：`stage_changed(str)`、`progress(int,int)`、`log_message(str)`、`loss_update(dict)`、`epoch_done(int,int)`、`batch_done(int,int,int)`、`finished(bool,str)`、`error(str)` | `workers.py:14-22` | `[S]` | — | |
| 7.10 | `request_stop()` 只置 `_stop_requested` 并通知（若 trainer 已建）`trainer.stop()`，emit `stage_changed("正在停止")` + `log_message("收到停止请求，将在当前步骤结束后保存退出")`；**不清 trainer 引用** | `workers.py:31-40` | `[S]` | — | |
| 7.11 | `_run_impl` 步骤映射固定：`preprocess`→[预处理]、`f0`→[F0]、`feature`→[特征]、`train`→[训练]、`all`→[预处理,F0,特征,训练]；每步后 `_stop_requested` 则 emit `finished(False,"已停止")`；全完 emit `finished(True,"流程完成")` | `workers.py:78-93` | `[S]`+`[D]` | — | |
| 7.12 | 单独步骤（非 `preprocess`/`all`）启动前做一致性检查：`manifest_diff_reason(exp_dir, input_dir, sr, 3.7)` 非空则抛 `RuntimeError(f"实验目录与当前输入不一致：{reason}。请先重新执行预处理")`；`per` 固定 3.7 | `workers.py:71-76` | `[D]` | — | |
| 7.13 | `TrainWorker.run()` 异常语义：`_stop_requested` 时 emit `finished(False,"已停止")` 不弹错误；否则 `error.emit(最后一行 traceback)` + `log_message.emit(完整 traceback)` + `finished(False,"训练失败")` | `workers.py:46-57` | `[S]` | — | |
| 7.14 | 训练阶段日志文案固定：`开始预处理音频`/`预处理完成`/`开始提取 F0`/`F0 提取完成`/`开始提取 HuBERT 特征（{hubert}）`/`注意：若本实验目录之前用另一种特征器提取过特征，请先删除 3_feature768 目录再重跑`/`HuBERT 特征提取完成`/`训练样本数: {count}`/`开始训练模型`/`模型已导出: {output}` | `workers.py:95-166` | `[D]` | — | |
| 7.15 | `count == 0` 抛 `RuntimeError("没有可训练样本")`；`pretrain_g/pretrain_d` 路径非空但不存在抛 `RuntimeError(f"预训练模型不存在: {path}")` | `workers.py:136-143` | `[D]` | — | |
| 7.16 | `exp_dir = TRAIN_LOGS_ROOT / exp_name`（项目根 `logs/`，与 cwd 无关）；`sr = parse_sr(options["sr"])`；`TrainConfig(... fp16_run=config.is_half, device=config.device ...)`；`tqdm`-free `_trainer.train()` 后 `finally cleanup()` | `workers.py:62-69,145-166` | `[D]` | — | |
| 7.17 | `TrainGuiState` dataclass 字段与默认值：`exp_name=""`、`input_dir=""`、`sample_rate="48k"`、`epochs=200`、`batch_size=4`、`save_every=20`、`learning_rate="0.0001"`、`pretrain_g=""`、`pretrain_d=""`、`hubert="chinese"`；`to_dict` 用键 `sr`（非 `sample_rate`） | `train_state.py:10-55` | `[S]`+`[D]` | — | ⚠ 学习率状态默认 `"0.0001"` vs 控件展示 `"1e-4"`，见 §11 |
| 7.18 | `TrainController.validate_options(raw)`：空实验名→`ValueError("实验名不能为空")`；空/不存在目录→`ValueError("请选择有效的音频目录")`；`float(lr)` 失败→`ValueError("学习率格式不正确")`；成功返回规范 dict（lr 已转 float） | `train_controller.py:65-92` | `[S]`+`[D]` | — | 文案固定 |
| 7.19 | 训练窗口 `closeEvent`：先 `_save_cfg`（失败静默）；`worker` 运行中 → `request_stop` + `wait(2000)`，超时 `event.ignore()` + 提示 `任务正在停止，请稍候再关闭窗口`；`_tool_thread` 同理但 `wait(5000)` + `工具任务正在停止，请稍候再关闭窗口`；否则 `event.accept()` | `train/view/window.py:232-250` | `[M]` | ▲ 无护栏 | |
| 7.20 | `on_batch` 计算 it/s（首 batch 或 `_batch_t0` 为 None 时置 0）与 ETA（`remaining_batches = (total-batch) + max(epochs-epoch,0)*total`，`eta = remaining/it_s`，格式 `· ETA {h}h{m}m`）；`epoch_label` 组合文案 `Epoch: {e} / {epochs} · Batch {b}/{total} · {it_s:.1f} it/s{eta}` | `train/view/window.py:179-197` | `[S]` | — | |
| 7.21 | `on_loss` 150ms 节流；文案 `Loss: D {:.4f} | G {:.4f} | Mel {:.4f} | KL {:.4f} | FM {:.4f}`，`loss_total` 非 None 时追加 ` | 总 {:.4f}`；内容不变时不重设标签 | `train/view/window.py:199-215` | `[S]` | — | |
| 7.22 | `on_finished(success,msg)`：`_set_running(False)`、`stop_btn.setText("停止训练")`、`_batch_t0=None`、`on_log(msg)`；成功 → success 样式 + `QMessageBox.information("完成", msg)`；失败 → error 样式（不弹成功框） | `train/view/window.py:221-230` | `[S]` | — | |
| 7.23 | `stop_training`：`request_stop()` + `stop_btn` 禁用 + 文案 `"停止中..."` + `stage_label.setText("当前阶段: 正在请求停止")` | `train/view/window.py:81-86` | `[S]` | — | |
| 7.24 | 工具操作走 `ToolThread(QThread)`（单信号 `done(bool,str)`，str 结果或 `"操作完成"`；异常 emit `(False, str(e))`）；每个工具在启动前若 `_tool_thread` 在运行则 `wait()` | `train/view/widgets.py:7-22`、`tools_merge.py:87-91`、`tools_inspect.py:61-65` | `[S]` | — | |
| 7.25 | 模型合并 UI：A/B 路径 + 比例 `QSlider(0..100)`（初值 50，label `"{a}:{v}% {b}:{100-v}%"`，缺名回退 `A`/`B`）+ 输出名 `QLineEdit("merged")`；校验空字段/文件不存在分别弹 warning；输出路径 `merge_output_path(name) = MODELS_DIR/{name}.pth`；成功后提示 `模型已保存到 assets/models/{name}.pth` | `tools_merge.py:14-101`、`train_controller.py:39-43` | `[D]` | — | |
| 7.26 | 修正真名成功后，若检视框指向同一文件（`Path.resolve()` 相等）则自动重跑 `_run_inspect`；zip 改名成功同理 | `tools_fixinfo.py:61-68`、`tools_inspect.py:99-105` | `[S]` | — | ⚠ 自动重跑逻辑保留，但 `_on_inspect_done` 的解析目标行受 §12-② 影响（改读 zip 原名行） |

## 8. autodl 云训练向导

| # | 行为断言 | 证据(文件:行) | 验证方式 | 护栏 | 备注 |
|---|---|---|---|---|---|
| 8.1 | 流程固定：`os.chdir` → `log.attach(DATA_ROOT/logs/autodl_train.log)` → `第 1 步 环境检查`（平台→磁盘→依赖→GPU→ffmpeg）→ `第 2 步 预训练模型检查`（`_probe_assets`）→ `run_wizard` → `print_summary` → `ask_yes("开始训练？（训练中 Ctrl+C = 本轮跑完保存后退出）", default=True)` | `autodl/__main__.py:42-60` | `[D]` | — | section 标题文案固定 |
| 8.2 | 依赖检查必需包：`torch/numpy/scipy/transformers`（`importlib.util.find_spec` 判在否）；缺失即打印安装指引并 `return EXIT_ENV(1)`；可选包 `PySide6/sounddevice` 缺失只提示 | `autodl/__init__.py:33-42`、`envcheck.py:52-79` | `[D]` | — | 不真正 import 重型模块 |
| 8.3 | GPU 探测：`torch.cuda.is_available()` 为假时 log WARN 并返回 `("cpu", False, 0.0)`（**CPU 降级，不致命**）；否则 `device="cuda:0"`；算力 `cc[0] < 7` 时 `use_fp16=False` 并 WARN | `envcheck.py:82-106` | `[D]` | — | 与 3.37（rvc 层无 CUDA 即崩）语义相反 |
| 8.4 | ffmpeg **三级定位**顺序固定：① 环境变量 `RVC_FFMPEG`（须存在）→ ② `shutil.which("ffmpeg")`（系统 PATH）→ ③ **仅 `os.name=="nt"`** 时 `assets/ffmpeg/ffmpeg.exe`；都无则返回 `("", "")` | `envcheck.py:109-120` | `[D]` | — | README 描述的顺序与实现一致 |
| 8.5 | 定位到的 ffmpeg 必须 `-version` 实测（timeout=15）：非零退出或 OSError → 返回 `""` 并 log ERROR（文案含 `apt install -y ffmpeg`）；成功 log `ffmpeg      : {path}（来自 {source}）`，`source` ∈ {`环境变量 RVC_FFMPEG`, `系统 PATH`, `项目内 assets/ffmpeg/ffmpeg.exe`} | `envcheck.py:123-147` | `[D]` | — | |
| 8.6 | `_require_ffmpeg`：素材若有非 `{.wav,.flac,.ogg}` 文件且**重新定位**仍无 ffmpeg → 抛 `EnvFatal`（文案含 `apt install -y ffmpeg`）；全为原生格式时 log `素材格式    : 全部为 wav/flac/ogg（libsndfile 可直接读取，N 个）` | `envcheck.py:150-162`、`autodl/__init__.py:46` | `[D]` | ▲ 无护栏 | ⚠ 用 `_locate_ffmpeg()[0]` 重新定位，忽略传入的 `cfg["ffmpeg"]` |
| 8.7 | `_probe_assets` 致命项（缺任一 → `EnvFatal("模型权重缺失")` → 退出码 1）：`assets/configs/{40k,48k}train_config.json`、`assets/hubert/{base,chinese}/` 三件套、`assets/rmvpe/rmvpe.pt`；底模缺失仅 WARN（`ready` 字典记录每采样率是否齐备） | `envcheck.py:165-219` | `[D]` | ▲ 无护栏 | ⚠ 返回值 `ready` 无调用方使用（见 §11） |
| 8.8 | `_probe_dataset`：文件数/总体积；用 `ThreadPoolExecutor(max_workers=min(8, len))` 对**前 300 个**（`limit=300`）经 `read_audio_info` 抽样统计总时长与采样率分布，`scanned<总数` 时按比例外推并标注 `（抽样估算）`；`<10` 文件 WARN、总时长 `<300s` WARN | `envcheck.py:248-285` | `[D]` | — | |
| 8.9 | `_suggest_batch(vram_gb, sr_hz)`：显存 ≥24→8、≥16→6、≥12→4、≥8→3、否则 2；`sr_hz==40000` 时 `+2` | `envcheck.py:222-233` | `[S]` | — | |
| 8.10 | `_scan_audio` 用 `os.walk` 递归收集扩展名 ∈ `{.wav,.mp3,.flac,.ogg,.m4a,.aac,.wma,.opus}` 的文件，结果 `sorted` | `envcheck.py:236-245`、`autodl/__init__.py:44` | `[D]` | — | |
| 8.11 | 向导提问共 **11** 次（1 次为数据目录可被 argv 跳过，故 10 或 11）且顺序固定：①数据集路径（`check=_dir_ok`：目录存在且含音频）②实验名（默认 `test`）③采样率 `40k/48k`（默认 `48k`）④HuBERT `base/chinese`（默认 `chinese`）⑤batch size（默认 `_suggest_batch`）⑥学习率（默认 `1e-4`，check `0<v<=0.1`）⑦训练轮次（默认 250）⑧保存间隔（默认 20）⑨保留 checkpoint 组数（默认 1）⑩是否用预训练底模 Y/n（默认 True）⑪是否开始训练 Y/n（默认 True） | `autodl/wizard.py:44,50,72,83,89,92,111,114,117,120`、`autodl/__main__.py:60`、`autodl/__init__.py:54-65` | `[D]` | — | 续训检测**不提问** |
| 8.12 | 交互输入语义：`ask` 默认非空时空回车取默认；`default` 为空且回空 → 打印 `  × 该项必填，请重新输入` 循环；`cast` 失败打印 `  × 格式不对：{raw}`；`check` 失败打印 `  × {msg}`；`EOFError` 时（有默认或 `allow_blank`）取默认，否则抛 `EnvFatal`（文案含「非交互运行时把答案按行喂给 stdin」）；`KeyboardInterrupt` 抛 `Cancelled` | `autodl/prompts.py:9-48` | `[D]` | — | |
| 8.13 | `ask_yes` 提示 `[{Y/n}]` 或 `[{y/N}]`；空串取默认；接受 `y/yes/是/1` → True、`n/no/否/0` → False；其他打印 `  × 请输入 y 或 n` 循环；`EOFError` 取默认、`KeyboardInterrupt` 抛 `Cancelled` | `autodl/prompts.py:51-68` | `[D]` | — | |
| 8.14 | `_as_sr` 接受 `48k/48/48000`（含去 `hz`、大小写不敏感）并统一返回 `"{n}k"`（≥1000 时 `//1000`）；`_human_size` 单位序列 `B/KB/MB/GB/TB`，`B` 取整无小数；`_human_dur` <60s→`{n}s`、<3600→`{m}m{ss:02d}s`、否则 `{h}h{mm:02d}m` | `autodl/prompts.py:79-102` | `[D]` | — | |
| 8.15 | 采样率为 `40k` 时 log WARN `40k：显存占用更小、训练更快，高频细节略少于 48k`；选 `chinese` 时两条 WARN（含「推理时模型卡牌上的「特征器」必须同样选 chinese」）；学习率 `>5e-4` 或 `<1e-5` 各一条 WARN | `autodl/wizard.py:74-76,84-86,98-101` | `[D]` | — | |
| 8.16 | 续训检测（不提问）：`_detect_ckpt_epoch` 取 `4_checkpoints/` 下 `G_*`/`D_*` 最新（回退 exp 根目录），两者都在才返回 `min(G,D)`，否则 0；检测到则 log `检测到已有 checkpoint（已训练到 epoch N）→ 自动续训` + `要重新训练请先删掉: {exp_dir}/4_checkpoints`；实验目录存在但无 ckpt 则 log 复用切片/特征 | `autodl/wizard.py:12-21,59-64` | `[D]` | — | |
| 8.17 | 训练轮次校验依赖 ckpt：`value<1` → `至少 1 轮`；`ckpt_epoch and value<=ckpt_epoch` → `已训到 epoch N，总轮次必须大于它（填的是目标终点，不是新增轮数）` | `autodl/wizard.py:104-111` | `[D]` | — | |
| 8.18 | 底模路径按 `sr_k = sr_hz//1000` 拼 `f0G{sr_k}k.pth`/`f0D{sr_k}k.pth`；仅当 `use_pretrained and 文件存在` 才写入 `cfg["pretrain_g/d"]`，否则空串；`use_pretrained=False` 时 log WARN「从随机权重开始训练…」 | `autodl/wizard.py:120-146` | `[D]` | — | |
| 8.19 | `cfg` 键固定：`device/fp16/ffmpeg/input_dir/exp_name/exp_dir/model_dir/sr/sr_hz/epochs/batch_size/lr/save_every/per=3.7/keep_models/keep_ckpts/ckpt_epoch/hubert/use_pretrained/pretrain_g/pretrain_d`；`per` 与 `keep_models`/`keep_ckpts` 默认来自 `DEFAULTS`（`keep_*` 读 `TrainConfig` 字段默认） | `autodl/wizard.py:31,128-147`、`autodl/__init__.py:54-65` | `[S]`+`[D]` | — | |
| 8.20 | `print_summary` 输出 14 行固定键值（数据集/实验名/实验目录/模型目录/采样率/特征器/总轮次/batch size/学习率/保存间隔/切片时长/保留 checkpoint/设备/底模），每行 **同一内容输出两遍**：`log.plain("  {key:<14} {value}")` 与 `log.log("配置 {key}: {value}")`；末尾 `log.plain(BAR)` | `autodl/wizard.py:151-173` | `[D]` | — | 双写是刻意（屏显对齐 + 日志可读） |
| 8.21 | 跳过逻辑：`manifest_diff_reason` 非空才 `step_preprocess`，否则 log `切片与素材一致，跳过预处理（要重建请删 {exp_dir}/manifest.json）`；`_features_ready(exp_dir)`（每个 `0_gt_wavs/*.wav` 的 stem 都在 `2a_f0/*.npy`、`2b-f0nsf/*.npy`、`3_feature768/*.npy` 中）为真则 log `F0 与 HuBERT 特征已完整，跳过提取`，否则依次 `step_f0`+`step_feature` | `autodl/__main__.py:68-83`、`steps.py:111-120` | `[D]` | — | |
| 8.22 | 步骤日志标题固定：`步骤 1/4 预处理（切片 + 重采样）`、`步骤 2/4 提取 F0（RMVPE）`、`步骤 3/4 提取 HuBERT 特征（{hubert}）`、`步骤 4/4 训练`；特征步骤带 WARN「若实验目录之前用另一种特征器提取过特征，请先删除 3_feature768 再重跑（已存在的特征文件会被跳过）」 | `steps.py:58,93,104,107,136` | `[D]` | — | |
| 8.23 | 进度回调 `_make_progress`：仅当 `pct%5==0 or done==total` 才输出；行格式 `{label}: {done}/{total} ({pct}%)  速度 {speed:.1f} 条/s  剩余 {human}`，level 为 `"进度"` | `steps.py:39-52` | `[D]` | — | |
| 8.24 | 预处理后统计：切片数、约音频时长（`bytes/2/sr_hz`）、体积；`len(gt) < 50` 时 WARN `切片数偏少（<50），训练容易过拟合，建议补充素材` | `steps.py:67-72` | `[D]` | — | |
| 8.25 | 若 `_detect_ckpt_epoch==0`（本次从头训练），删除 `model_dir` 下所有 `{exp_name}_e*.pth` 并 log `本次从头训练，已删除旧的导出模型 N 个（epoch 号会重新计数）` | `steps.py:143-149` | `[D]` | — | |
| 8.26 | 训练样本数为 0 → `RuntimeError("没有可训练样本（特征或 F0 缺失），请删除 {exp_dir} 后重跑")`；预训练路径不存在 → `RuntimeError("预训练模型不存在: {path}")` | `steps.py:151-159` | `[D]` | — | |
| 8.27 | `step_train` 的 epoch 汇总行固定：`epoch {epoch:>4}/{total} | {lr_now}D {...:.4f} G {...:.4f} Mel {...:.4f} KL {...:.4f} FM {...:.4f} | {secs:.1f}s {speed} 剩余 {eta}{mem}`，level `"EPOCH"`；`lr_now` 形如 `lr {:.2e} | `（仅当 trainer 有 optim_g）；`mem` 形如 ` | 显存 现{:.1f}/峰{:.1f}GB`（仅 cuda）；ETA 用 EMA（`ema = ema*0.7 + secs*0.3`） | `steps.py:196-215` | `[D]` | — | |
| 8.28 | `on_trainer_log`：以 `"epoch "` 开头的 trainer 自带行只 `file_only`（不进屏），其余 `log.log` | `steps.py:217-221` | `[D]` | — | |
| 8.29 | 信号处理：SIGINT/SIGTERM 首次置 `STOP.requested` 并（若已建）调 `trainer.stop()`，log `收到 SIGINT (Ctrl+C) → 当前 epoch 跑完会保存后退出`；**二次**信号 log `再次收到终止信号，强制退出` 并 `sys.exit(EXIT_RUNTIME)` | `steps.py:21-36` | `[M]`+`[D]` | ▲ 无护栏 | |
| 8.30 | 结束输出固定：section `全部完成` + `总耗时      : {elapsed}`；若 `model_dir/*.pth` 非空则 `导出模型目录: {dir}` + 每模型一行 `  · {name}  {size}`；再 `checkpoint  : {exp_dir}/4_checkpoints`、`训练日志    : {exp_dir}/train.log`、`向导日志    : {log.log_file}`；返回 `EXIT_OK` | `autodl/__main__.py:88-99` | `[D]` | — | `elapsed` 格式 `%Hh%Mm%Ss` |
| 8.31 | 异常兜底：`Cancelled` → log WARN `已取消` + `EXIT_OK`；`EnvFatal` → 逐行 ERROR + `环境检查未通过，已中止（不会开始训练）` + `EXIT_ENV`；`KeyboardInterrupt` → log WARN `已中断` + `EXIT_RUNTIME`；其他异常 → log ERROR `运行出错: {type}: {exc}` + 完整 traceback + `常见问题：显存不足（OOM）请调小 batch size；素材格式问题请确认 ffmpeg 已装` + `EXIT_RUNTIME`；`finally` 恒 `log.close()` | `autodl/__main__.py:101-121` | `[D]` | — | 文案固定 |
| 8.32 | 产物根三级回退：`RVC_OUT_ROOT`（strip 非空）→ `/root/autodl-tmp`（存在时）→ `<PROJECT_ROOT>/autodl-tmp`；`LOGS_ROOT=DATA_ROOT/logs`、`MODELS_ROOT=DATA_ROOT/models` | `autodl/__init__.py:21-30` | `[D]` | — | |
| 8.33 | `_probe_platform`/`_probe_disk` 输出行固定：`系统        : {system} {release}`、`Python      : {ver}  ({executable})`、`CPU / 内存  : {n} 核 / {mem:.1f} GB`（`os.sysconf` 不可用时退化为 `CPU         : {n} 核`）、`项目根目录  : {PROJECT_ROOT}`、`产物根目录  : {DATA_ROOT}`、`{系统盘|产物盘}剩余   : {free} / {total}  ({path})`（<5GB WARN） | `envcheck.py:25-49` | `[D]` | — | |

## 9. 通用质量契约

| # | 行为断言 | 证据(文件:行) | 验证方式 | 护栏 | 备注 |
|---|---|---|---|---|---|
| 9.1 | 异常体系（继承关系固定）：`RVCError(Exception)` 为基类；`ModelLoadError`、`AudioDeviceError`、`InferenceError`、`ConfigError`、`AudioLoadError` 直继 `RVCError`；`F0ExtractionError`、`FeatureExtractionError` 继 `InferenceError` | `errors.py:11-41` | `[S]` | — | |
| 9.2 | 异常使用边界：环境问题（ffmpeg 缺失）用 `FileNotFoundError`；数据问题（解码失败）用 `AudioLoadError`；WAV 头解析失败用**裸 `ValueError`**；`read_audio_info` 非 WAV 正则失配用 `ValueError`；`write_wav` 非法 subtype 用 `ValueError`；设备问题用 `AudioDeviceError`；模型加载用 `ModelLoadError`；无 GPU 用 `RuntimeError` | `audio_file.py:33-37,65-67`、`wav_file.py:51,91,111,156`、`stream.py:58-66,85-88`、`sessions.py:75`、`device.py:45` | `[S]` | — | 重写不得把裸 ValueError 换成 RVCError |
| 9.3 | 依赖方向八层单向无环：`core` ← `runtime` ← `dsp` ← `nn` ← (`models` / `io`) ← `pipeline` ← (`streaming` / `train`) ← (`gui` / `autodl`)；`rvc/` 不依赖 `gui/`；`gui/*/view/*` 不顶层 import `rvc.*` | `rvc/dsp/__init__.py:1-5`、`runtime/__init__.py:1-15`、`models/__init__.py:1-15`、`window.py:47-49`、`README.md:92-115` | `[S]` | — | AST 可静态守护（原 `tests/test_architecture.py` 已删，重写需重建等价断言） |
| 9.4 | `rvc/runtime/__init__.py` 惰性导出 `Config`（`__getattr__` 转发到 `runtime.device`），`paths` 相关函数（`config_path`/`train_config_path`）顶层即导出；访问未知属性抛 `AttributeError` | `runtime/__init__.py:6-15` | `[S]` | — | `Config` 依赖 torch，惰性避免启动加载 |
| 9.5 | `rvc/models/__init__.py` 惰性导出 `load_hubert`（`__getattr__`），未知属性抛 `AttributeError` | `models/__init__.py:6-15` | `[S]` | — | |
| 9.6 | 路径单源：所有 assets 路径只在 `rvc/runtime/paths.py` 声明一次，均以 `PROJECT_ROOT = Path(__file__).resolve().parents[2]` 为基（与 cwd 无关）；`PROJECT_ROOT/assets`、`CONFIG_ROOT`、`STATE_FILE`、`TRAIN_CONFIG_FILES`、`HUBERT_ROOT`、`RMVPE_PATH`、`PRETRAINED_ROOT`、`MODELS_DIR`、`TRAIN_LOGS_ROOT`、`FFMPEG_EXE`、`FFPROBE_EXE`、`RESOURCES_DIR`、`ICON_IDLE_PATH`、`ICON_ACTIVE_PATH` | `paths.py:9-43` | `[S]` | — | |
| 9.7 | `parse_sr` 唯一解析入口：`"48k"`→48000、`"48000"`→48000；`train_config_path(sr)` 对不支持/缺省 sr 回退 48k，目标文件不存在抛 `FileNotFoundError` | `paths.py:50-70` | `[D]` | — | |
| 9.8 | 惰性导入约定：`rvc.train.*` 与 `rvc.streaming.engine` 在 GUI 窗口构造期不被导入（torch 只在 worker/预热线程内加载，保证窗口秒开）；`rvc/nn`、`rvc/dsp`、`rvc/io` 顶层不导入 torch 重型链路 | `workers.py:1-5,60-61`、`lifecycle.py:83-97`、`engine_controller.py:39-49`、`devices.py:1-3` | `[S]` | — | |
| 9.9 | 无新增第三方依赖：`requirements.txt` 为依赖唯一来源，`Retrieval-based-Voice-Conversion-WebUI/` 不在引用范围内 | `requirements.txt`、`README.md:24-37` | `[S]` | — | 重写不得引入新包 |
| 9.10 | 无 CPU 回退契约（推理侧）：`Config` 在 `torch.cuda.is_available()` 为假时抛 `RuntimeError` 而非降级；`is_half=True` 硬编码；设备硬编码 `"cuda:0"` | `device.py:26-27,41-49` | `[S]` | — | 与 autodl 的 CPU 降级（8.3）刻意相反 |

---

## 10. 现状即 bug、但属于对外可观测行为、重写必须原样保留

> 这些条目**最容易被「顺手修好」从而违约**。重写时**必须保持现状**，除非用户另行裁定。

| 主条目 | 行为 | 为什么容易被误修 | 证据 | 护栏 |
|---|---|---|---|---|
| 2.14 | 实时音频回调吞异常：清零输出、继续运行、计入 `error_count`（连续 3 次才 `CallbackStop`） | 看似「应该立即停/上抛」，实为现状契约（GUI 靠 `error_lbl` 展示） | `engine.py:202-211`、`state.py:103-109` | ▲ 无护栏 |
| 2.15 / 11.1 | `process_file` 会替换 `self._runner` 并**原地**改 `runtime_params`（实时运行中调用即互相干扰） | 看似「应加锁/应独立实例」，但用户已裁定保持「离线与实时共用同一 engine」语义 | `engine.py:215-244` | ▲ 无护栏 |
| 3.15 | F0 每块从**完整** `input_wav_16k` 全量重提（不做增量缓存，计算冗余） | 看似「应恢复滚动缓存提速」，但 f800ec3 刻意删除缓存以换取帧对齐 | `tracker.py:16-67`、`pipeline.py:207-239` | ▲ 无护栏 |
| 3.23 | 静音/UV 帧离散化到 **1**（注释写 0=UV，实现是 1） | 看似「注释与实现不一致，应改回 0」——改 0 会破坏与合成器训练/官方模型的数值口径 | `postprocess.py:164-194` | ▲ 无护栏 |
| 3.30 | `apply_sola` 返回值与入参**共享存储且已被原地改写**（NaN 路径才 clone） | 看似「应返回独立张量」，改动会改变上游复用行为 | `sola.py:60-76` | ▲ 无护栏 |
| 3.47 | FCPE 噪声前缀表含疑似 typo 的 `"[INF0]"` | 看似「应改成 `[INFO]`」——但 `[INFO]` 已单独存在，改字会改变过滤集合 | `extractor.py:52-60` | ▲ 无护栏 |
| 4.7 | `read_audio_info` 非 WAV 正则失配时返回 `samplerate=0/channels=0/frames=0`（半失败不报错） | 看似「应抛异常」，但 `_probe_dataset` 依赖其容错 | `wav_file.py:161-181` | ▲ 无护栏 |
| 4.4 | `write_wav` PCM_16 对越界样本**静默 clip**，不报错 | 看似「应报错」，但这是与上游 soundfile 一致的既定语义 | `wav_file.py:45-51` | ▲ 无护栏 |
| 4.6 | `read_wav_info` 抛**裸 `ValueError`**（不进 RVCError 体系） | 看似「应统一到 RVCError」，GUI 工具页已按 ValueError 捕获 | `wav_file.py:82-91,111` | ▲ 无护栏 |
| 3.20 | `median_filter_f0` 对非 1-D / 过短 / kernel<3 输入**静默原样返回** | 看似「应报错」，实为实时回调兜底约定 | `postprocess.py:152-153` | ▲ 无护栏 |
| 3.7 | 16k 重采样**丢弃修正帧**（只写当前块，不回写上一块最后 10ms） | 看似「应写回修正帧消除误差」，但写回会导致 HuBERT 特征漂移（旧「凤凰→废黄」回归） | `runner.py:272-278` | ▲ 无护栏 |
| 3.29 | `fast_rms` 输入过短时抛 `ValueError`（中文文案）；`apply_rms_mix` 不兜底 | 看似「应 pad 或返回 0」，但这是明确契约 | `rms.py:27-32` | ▲ 无护栏 |
| 8.6 | `_require_ffmpeg` 用 `_locate_ffmpeg()` **重新定位**，忽略已下发的 `cfg["ffmpeg"]`（潜在不一致） | 看似「应复用 cfg 值」，改动会改变定位来源判定 | `envcheck.py:150-162` | ▲ 无护栏 |
| 8.7 / 11.8 | `_probe_assets` 返回的 `ready` 字典**无任何调用方使用**（死返回） | 看似「应删除或接入」，删除改变 `_probe_assets` 签名 | `envcheck.py:165-219`、`__main__.py:55` | ▲ 无护栏 |
| 5.38 | 训练 JSON 的 `log_interval` 字段**存在但无任何代码引用**（死字段） | 看似「应删掉」，但删字段改变配置文件字节/兼容性 | `assets/configs/*.json:3`、`trainer.py`（无引用） | ▲ 无护栏 |
| 6.23 | 「开始」警告文案含旧词 `参数调节`（页签已改名「音色调节」，离线警告已用新名） | 看似「应统一改名」，但文案属对外可观测行为 | `lifecycle.py:135` vs `offline_controller.py:82` | ▲ 无护栏 |
| 3.4 / 2.4 | `load_model` 无条件清 F0 CUDA Graph 缓存（缓存命中时也清） | 看似「应挪到 miss 分支」，但清图时机影响快速 stop/start 的音质 | `engine.py:92-98` | ▲ 无护栏 |
| 3.36 | `gpu_mem = int(bytes/1024^3 + 0.4)`（+0.4 取整，非四舍五入） | 看似「应 `round()`」，改动改变显示值 | `device.py:54-56` | ▲ 无护栏 |
| 3.27 | formant 重采样核缓存 ≥64 时**整表 clear**（非 LRU 淘汰单个） | 看似「应淘汰最旧」，改动改变缓存行为 | `synthesis.py:75-77` | ▲ 无护栏 |

## 11. 待用户裁定 → 处置结论（按默认策略收敛）

> 用户 2026-09-23 裁定：本轮**不再逐条答复**。统一默认策略——凡「这是有意的吗？」一类问题，一律**保持现状、对外行为不变**，仅内部形态按质量规范重写（单一职责 / 显式优于隐式 / 不用泛化命名）。原 18 条已**全部收敛为「处置结论」**（原「为什么判断不了」列删除）。
> 其中 **11.1、11.9、11.10、11.16、11.17** 五条此前已被用户单独裁定，直接采用其裁定结果。

| # | 位置 | 现象 | 处置结论 |
|---|---|---|---|
| 11.1 | `engine.py:215-244` | `process_file` 复用同一 engine、替换 `_runner`、原地改 `runtime_params` | **用户已裁定：保持「离线与实时共用同一 engine」语义**。对外行为不变；重写**不得**为此新增锁 / 独立实例 / 状态标记。内部：把「替换 `_runner` + 原地 `update_from`」的意图用显式命名与注释表达，副作用序列不变 |
| 11.2 | `engine.py:87-98` | `load_model(force=False)` 命中缓存仍清 F0 图缓存 | 保持现状（清图时机影响快速 stop/start 音质，属对外可观测）。内部：把「无条件清图」提为显式步骤并命名，消除「顺手前置」的隐式感 |
| 11.3 | `trainer.py:116-117` | `ExponentialLR(last_epoch=start_epoch-2)` 的 `-2` | 保持现状（`-2` 属训练数值对外行为）。内部：加注释说明对齐上游 lr 轨迹，禁止后人「修正」为 `-1` |
| 11.4 | `autodl/__init__.py:54-65` | epoch 默认三处不一：向导 250 / GUI 200 / dataclass 2000 | 保持现状（三层各自默认值对外可见，均不变）。内部：以具名常量集中声明各层默认、消除魔法数字，取值不变 |
| 11.5 | `autodl/__init__.py:57` | `save_every` 默认 20（向导/GUI）vs `TrainConfig.save_every_epoch=200` | 保持现状（各层默认均不变）。内部：同上收敛为具名常量 |
| 11.6 | `extract_feature.py:24` | `HuBERTExtractor.__init__` 默认 `"base"`（vs `HUBERT_DEFAULT="chinese"`） | 保持现状（两侧调用方均显式传参，默认值不被使用，对外无影响）。内部：默认值收敛到具名常量 |
| 11.7 | `train_state.py:21,24` vs `settings_tab.py:73` | lr 状态默认 `"0.0001"` vs 控件初值 `"1e-4"` | 保持现状（字符串差异对外可见）。内部：**不得**统一格式化（会改变控件初始文本） |
| 11.8 | `envcheck.py:165-219` | `_probe_assets` 返回 `ready` 却无调用方 | 保持现状（保留返回值与签名；删除会改变函数对外签名） |
| 11.9 | `envcheck.py:109-120` | ffmpeg 三级定位第三级仅 `os.name=="nt"` | **用户已裁定：保持 Windows 硬编码**（Linux 兼容只在 autodl 层处理）。内部：平台判断显式化并注明「有意为之」 |
| 11.10 | `devices.py:5-13` | 依赖 sounddevice 私有 API `sd._terminate()/_initialize()` | **用户已裁定：保留私有 API**。但 `gui/infer/controller/device_controller.py:22-23` 的**重复内联调用必须改为复用 `rvc/io/devices.py` 的包装**（终止/重扫唯一入口）；刷新设备列表的对外结果不变 |
| 11.11 | `offline_controller.py:122-139` | 完成/错误后不 `deleteLater()/wait()`，仅保留引用 | 保持现状（避免 Qt 销毁竞态）。内部：把该意图固化为显式生命周期契约注释 |
| 11.12 | `window.py:242-247` | 运行中禁止刷新设备（warning + return） | 保持现状（防杀流限制；warning 文案固定） |
| 11.13 | `extractor.py:143-157` | `F0Extractor.extract_raw` 基类默认实现（子类均重写） | 保持现状（保留基类默认实现，供未重写子类/第三方继承）。内部：显式标注「不准确，仅兜底」 |
| 11.14 | `synthesizer_model.py:52-53` | `sr2sr.get(sr, 48000)` 未知 str 静默回退 48k | 保持现状（静默回退不变）。内部：映射表与默认值收敛为具名常量 |
| 11.15 | `audio_file.py:41-71` | `load_audio` 的 `timeout=300` 不自适应输入时长 | 保持现状（不按输入时长自适应）。内部：300 提为具名常量并注释取舍 |
| 11.16 | `device.py:41-56` | 设备硬编码 `"cuda:0"`、无多 GPU 入口、`Config` 进程单例不可重配 | **用户已裁定：保持现状**（单 GPU 限定 + 无 CPU 回退 + 进程单例） |
| 11.17 | `paths.py:36-38` | rvc 层 ffmpeg 硬编码 Windows `assets/ffmpeg/ffmpeg.exe` | **用户已裁定：保持 Windows 硬编码**（与 11.9 同一裁定；Linux 兼容只在 autodl 层） |
| 11.18 | `wav_file.py:125-181` | `ffmpeg_path` 标注 `str \| None`，内部 `resolve_ffmpeg()` 返回 `Path` | 保持现状（运行时 str/Path 均可，对外无变化）。内部：按「显式优于隐式」统一标注为 `str \| os.PathLike \| None`（仅类型层） |

### 11.19 仍需拍板

**0 条。** 按默认策略收敛后，18 条全部落入「保持现状、对外行为不变」，**无需改变对外行为即可成立**，故本轮无遗留待拍板项。

> 说明（供架构师，**不影响对外契约**）：11.4 / 11.5 虽无需改对外行为，但「内部单一来源取哪个默认值」需架构师在重写时定夺——无论取何值，**三层各自对外默认值必须逐一保持不变**。11.15 的 300s 超时对超长输入可能不足，但放宽会改变对外行为，故维持现状。

---

## 12. 经用户批准的行为变更（唯一例外 · D-3 模型信息首行）

> 用户 2026-09-23 裁定：本轮**唯一**批准改变对外行为的项是 **D-3**（模型信息查看的首行展示）。**裁定原意：首行应优先显示 `info`。**
> 除本节外，§10 与 §11 全部条目一律保持现状——**本节是本轮唯一的「对外行为可变更」出口**。

**改动前现状**（已记录于 §5.27 / §7.7 / §7.26）

- `rvc/train/checkpoint.py:168-185` `inspect_model`：`archive = _zip_archive_name(path)`；`name = archive if archive else str(ckpt.get("info", Path(path).stem))`；**首行**输出 `真名/模型信息: {name}`（即 **zip 原名优先**）；`ckpt["info"]` 另起一行 `Info: {info_val}`（非空才出）。
- `gui/train/view/tabs/tools_inspect.py:68-77` `_on_inspect_done`：解析**首行**的 `真名/模型信息:`，把其值自动填入 `win.rename_edit`；`_run_rename_zip`（`:80-96`）再用该值调 `change_archive_name`。
- `gui/train/view/tabs/tools_fixinfo.py:14-58`「修正模型信息（真名）」页：修改 `ckpt["info"]`（`change_info`，`checkpoint.py:224-232`）。

| # | 条目 | 内容 |
|---|---|---|
| 12.1 | **①变更后的期望行为** | `inspect_model` **首行优先显示 info**：`info` 非空时首行展示 info（不再展示 zip 原名）；`info` 为空时首行回退为 zip 原名（若存在）否则文件 stem（保留原兜底链）。**zip 归档原名另起独立一行**输出。其余行（`文件大小` / `采样率` / `版本` / `F0 支持`）顺序与格式不变。行标签措辞由重写确定，但「**info 在首行**」「**zip 原名独立成行**」两条为硬约束 |
| 12.2 | **②连带影响之一：zip 原名必须独立成行** | 必须把 zip 归档原名输出到**单独一行**（如 `zip 原名: {archive}`）。若仍与 info 挤在同一行，任何 UI 都无法稳定读取它 |
| 12.3 | **②连带影响之二：`_on_inspect_done` 解析目标必须改行** | `tools_inspect.py:72-74` 的解析目标须从「首行」改为「**zip 原名所在行**」。否则 `rename_edit` 会被自动填入 **info**，而 `_run_rename_zip` → `change_archive_name` 会拿 info 当新原名，「修改 zip 原名」功能**退化为「改 info」**（现有流程被破坏） |
| 12.4 | **②连带影响之三：「真名」一词需消歧** | `tools_inspect.py`（label `zip 原名`）与 `tools_fixinfo.py`（label `新真名`）两处「真名」现同时指向 **info 字段**与 **zip 归档名**两个不同对象，必须消歧（例如统一为「模型信息（info）」与「zip 归档名」两套叫法），界面文案随之调整。<br>命名与架构师 P2-1 **共用同一张已定稿表**（两份文档逐字一致）：**info 一侧** —— `info` → `model_info`、`fix_info` → `model_info`、`change_info` → `change_model_info`、`_run_fixinfo` → `apply_model_info`；**zip 归档名一侧** —— `*_name_edit` / `*_archive*`（`archive_name_edit`、`_run_rename_archive`）。**硬约束**：「name / archive」字样只留给 zip 归档名，info 一侧一律叫「信息」（原提案 `fix_model_name_edit` 因语义倒错已弃用）；约束优先于具体取名 |
| 12.5 | **显式验收样例（真机肉眼比对用）** | 因本轮无自动化护栏（§13），D-3 只能靠真机比对收口，故把「改前 / 改后」输出文本固定为样例。**改前**：L1 `真名/模型信息: {archive 或 info 或 stem}`、L2 `Info: {info}`（非空才出）、L3 `文件大小: {:.1f} MB`、L4 `采样率: {sr}`、L5 `版本: {version}`、L6 `F0 支持: 是/否`。**改后（info 非空且有 zip 原名）**：L1 `模型信息: {info}`、L2 `zip 原名: {archive}`、L3–L6 不变。**改后（info 为空）**：L1 保持旧兜底链 `模型信息: {archive 或 stem}`，且 L2 `zip 原名: {archive}` 仍须输出（L1 与 L2 可能重复，属可接受冗余，**不得**为去重而省略 L2）。**实测方法**：取同一实验的两个 `.pth`（一个 info 非空、一个 info 为空）各点一次「查看」，肉眼确认 L1 内容与 L2 是否存在 |

**③被本节取代的原条目**：**§5.27**（`inspect_model` 输出行顺序断言）——其首行语义已变更，**标记为「已被 §12 取代」**，重写时以 12.1 为准；相关但**不**被取代、仅受 12.2/12.3/12.4 影响的条目：**§7.7**（工具页签三组标签）、**§7.26**（工具成功后自动重跑检视）。§5.29（`change_info`）行为不变。

---

## 13. 本轮验证策略（用户裁定）与「无护栏」标记

> 忠实记录用户 2026-09-23 裁定：
>
> 1. **本轮不搭建任何自动化测试 / 护栏 / 对拍夹具**——不恢复已删的 `tests/`，不新建最小 guard，不写 diff harness。
> 2. **阶段三不再逐步自验**（不要求实现者每步跑通、自证逐条一致）。
> 3. **最终验收 = 用户真机实跑**（实时转换 / 离线转换 / 训练 / autodl 向导各实跑一遍，以人耳、人眼判定）。
>
> 因此 §1–§9 主清单在「验证方式」之外新增 **`护栏`** 列，取值二选一：
>
> - `—` = **可静态 / 确定性复核**：重写后由人（或最小的 AST / 字节 diff 检查）按「证据」列逐条比对即可判定「一致 / 违约」，**不需要真机**。
> - `▲ 无护栏` = **无任何自动化护栏**：提交前无法自动判定，只能靠「静态对照 + **最终真机实跑**」兜底。**这些即本轮最高风险区。**

### 13.1 最高风险区（`▲ 无护栏`）标记规则与清单

机械、可复核的标记规则（本清单已按此机械执行）：

1. **全部 §3（48 条）+ §4（14 条）= 62 条**：实时推理链与音频 / DSP 数值路径，「逐样本 / 逐块一致」在无夹具条件下无法自动判定；
2. **§10 全部（19 条）**指向的主清单条目：「现状即 bug 必须保留」，最易被「顺手修好」，且无任何断言可拦；
3. **其余域中「验证方式」含 `[对拍]` 或 `[M]` 的条目（39 条）**：本质同样依赖真机 / 对拍，无护栏。

| 域 | 该域条目数 | 其中 `▲ 无护栏` |
|---|---|---|
| §1 入口与命令行 | 18 | 2 |
| §2 rvc 引擎公开 API | 20 | 8 |
| §3 实时推理链 | 48 | **48** |
| §4 音频 IO 与 DSP | 14 | **14** |
| §5 训练管线 | 40 | 14 |
| §6 推理 GUI | 39 | 14 |
| §7 训练 GUI | 26 | 2 |
| §8 autodl 云训练向导 | 33 | 3 |
| §9 通用质量契约 | 10 | 0 |
| **主清单合计** | **248** | **105** |
| §10 现状即 bug 必须保留 | 19 | 19（主清单子集，已含上行，不重复计数） |

**非 §3/§4 但同样 `▲ 无护栏` 的 39 条（按编号）**：1.3、1.8、2.4、2.8、2.9、2.11、2.14、2.15、2.16、2.19、5.1、5.2、5.3、5.4、5.5、5.6、5.11、5.12、5.19、5.33、5.35、5.36、5.37、6.1、6.14、6.15、6.18、6.19、6.20、6.21、6.22、6.24、6.25、6.28、6.33、6.39、7.1、7.19、8.29。
（其中 8.6、8.7、5.38、6.23 属 §10 独有、被本条规则兜入。）

> **最高风险区（去重后）= 105 条**
> = §3 全部 48 + §4 全部 14 + §10 独有 4（8.6、8.7、5.38、6.23）+ 其余含 `[对拍]`/`[M]` 的 39 条。

### 13.2 本轮验收口径（据此判定「一致 / 违约」）

- `—` 条目：重写后由人按「证据」列静态比对即可闭环；任一不一致即违约。
- `▲ 无护栏` 条目：**提交前不做逐条自验**，最终以用户真机实跑为准。**§3 / §4 / §10 中任一条被改变，都只可能在用户实跑时才暴露**——故重写时的默认动作是「**逐字逐值原样搬运，不做任何优化**」。
- 特别提示：§10「现状即 bug 必须保留」的 19 条必须原样保留；§3、§4 的数值链路（SOLA / RMS / 中值滤波 / 音域映射 / 离散化 / 重采样核）**禁止「顺手化简」**。
- §12（本轮唯一行为变更）已把「改前 / 改后」输出文本固化为样例（§12.5）：真机跑时按该样例逐行肉眼比对即可判定 D-3 是否落地；`_on_inspect_done` 是否仍读首行，可直接看「修改 zip 原名」是否被自动填入 info（§12.3）。

---

## 附：本清单相对上一轮《功能基线清单》的关键差异（供交叉核对）

| 变更来源 | 差异点 |
|---|---|
| `f800ec3` | 删除 pitch/pitchf/confidence 滚动缓存（旧 3.2 提到缓存）；F0 改为每块从完整缓冲区提取并取前 `p_len` 帧（新 3.14–3.15）；特征先 upsample 再对齐 F0（新 3.14）；`tests/` 删除（旧 9.4「65 用例」作废） |
| `d9f6a3b` | 训练模块导入崩溃修复（`rvc.train` 内不再顶层 import 重型链，改为步骤内惰性导入）；`offline_controller` 重写（进度 20+80 映射、worker 生命周期）；bindings/window 删除 `hasattr` 探测框架（控件缺失即 AttributeError）；设备索引越界改抛 `IndexError`（不再静默回退）；`export_dir` 显式参数取代 `WEIGHTS_DIR` 全局改写 |
| 本轮核实新增 | `RVC_CUDA_GRAPH` / `RVC_CUDA_GRAPH_MAX_CACHE` 环境变量通道**已删除**，改为 `RuntimeOptions` 显式对象（新 3.34）；GUI 页签改名（旧「参数调节/高级功能」→ 新「音色调节/性能调节」，新 6.2）；`BINDINGS` 实为 **13** 项（旧写 14，新 6.4）；`rvc.streaming.VoiceEngine` 不存在，正确路径 `rvc.streaming.engine.VoiceEngine`（新 §2 抬头）；F0 后处理顺序与手动真机无关的核心路径以 `[对拍]` 覆盖（新 3.17–3.23）；`EngineState`/`InferenceContext` 字段固定（新 3.x 证据）；新增 autodl `_locate_ffmpeg` 三级定位细节（新 8.4–8.5）；新增「现状即 bug 必须保留」专节（§10）、「待用户裁定 → 处置结论」专节（§11）、「经批准的行为变更（唯一例外 D-3）」专节（§12）、「本轮验证策略与无护栏标记」专节（§13） |
| 本轮删除（旧文档条目作废） | 旧 6.2 的 `save_state.json` 旧 `"experimental"` 键迁移逻辑：**代码中已无**，不得作为兼容性要求；旧 6.7/9.4/9.2 依赖的 `tests/` 与 `python -m tests.run_tests`：**已删除**；旧 5.7 的 `WEIGHTS_DIR` bug：**已修**（现为 `export_dir` 显式参数）；旧 1.6/3.9 的环境变量 CUDA Graph 开关：**已删** |

---

## 统计

| 域 | 条目数 | 其中 `▲ 无护栏` |
|---|---|---|
| 1 入口与命令行 | 18 | 2 |
| 2 rvc 引擎公开 API（VoiceEngine） | 20 | 8 |
| 3 实时推理链 | 48 | 48 |
| 4 音频 IO 与 DSP | 14 | 14 |
| 5 训练管线 | 40 | 14 |
| 6 推理 GUI | 39 | 14 |
| 7 训练 GUI | 26 | 2 |
| 8 autodl 云训练向导 | 33 | 3 |
| 9 通用质量契约 | 10 | 0 |
| **合计（主清单）** | **248** | **105** |
| §10 现状即 bug 必须保留（主清单子集，指向上表编号，不重复计数） | 19 | 19（已含于上行） |
| §11 处置结论（按默认策略收敛；仍待拍板 0 条） | 18 | — |
| §12 经批准的行为变更（唯一例外 D-3） | 5 | — |

> **最高风险区（`▲ 无护栏`）去重后 = 105 条** = §3 全部 48 + §4 全部 14 + §10 独有 4（8.6、8.7、5.38、6.23）+ 其余含 `[对拍]`/`[M]` 的 39 条。详见 §13。
