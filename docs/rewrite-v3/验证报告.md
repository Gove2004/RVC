# 验证报告（rewrite-v3 · 阶段四交付物）

> **验证策略（用户 2026-09-23 裁定，《功能基线清单》§13）**：本轮**不搭建任何自动化测试/护栏/对拍夹具**；阶段三不逐步自验；**最终验收 = 用户真机实跑**（实时转换 / 离线转换 / 训练 / autodl 向导各实跑一遍，以人耳、人眼判定）。
> 本报告如实汇总「已执行的确定性验证」与「只能由真机暴露的残余风险」，不夸大覆盖面。

## 1. 已执行的验证（静态 / 确定性）

| # | 验证项 | 方法 | 结果 |
|---|---|---|---|
| 1 | 提交纪律 | 每批 commit 正文含「变更 / 数值·时序影响 / 触及无护栏 / 依据」四行自检（D-R6） | 20 个 commit 全部合规 |
| 2 | delta 实测对账 | `git diff --name-status 97c1fed..HEAD` | **M 50（代码/README）+ A 1（contracts.py）= 保持 80 / 修改 50 / 新增 1 = 131**，与 §2.4 定案逐字吻合（见《重构对照表》头部） |
| 3 | S4 数值对照（81 条高危路径的载体，29 文件） | `git diff -U0 97c1fed..HEAD` 逐文件实测 + 逐 hunk 对照预期变更点 | **A 组 19/19 零漂移（diff 为空）；B 组 10/10 全部落在预期变更点内，0 行预期点外漂移**（明细见《S4-数值对照表》§5） |
| 4 | R-1 红线（F0↔特征帧对齐） | S4 明细 | `f0_raw[:p_len]`（tracker.py 代码 54–65）零触碰；`features.py` 全文未动 |
| 5 | R-2 红线（CUDA Graph 时序） | S4 明细 + D-R4 四维核对 | 写点 1（`configure_cuda_graph`→`apply`）、读点 2（`cuda_graph_enabled`、`_GraphCache.__init__`）、写/读时机均不变；`_GraphCache` 读 `max_entries` 的时机与取值、`_rvc_cuda_graph_cache` 猴补、`purge_cuda_graphs` 逐字未动 |
| 6 | D-3 落地验证（唯一行为变更） | venv 真实运行 `inspect_model`，按《功能基线清单》§12.5 两样例断言 | 通过：info 非空 → L1 `模型信息: {info}`、L2 `zip 原名: {archive}`；info 为空 → L1 回退 zip 原名（与 L2 值重复，可接受冗余）；L3–L6 不变；UI 解析目标 = zip 原名行（非 info） |
| 7 | NamedTuple 裁定实证（B3-3c-a） | 读 torch 2.11 `pin_memory.py:87-90` 源码 + 实证脚本（collate 数值 + 9/9 字段钉页） | `TrainSample`/`TrainBatch` 必须为 NamedTuple（dataclass 会使 `pin_memory=True` 静默失效）——实证全绿 |
| 8 | 语法完整性 | 每批 `ast.parse` 全部触碰文件 | 全绿（未拉起 PySide6/torch 的纯语法检查） |
| 9 | 越权自查 | 双判据（A：对外可见字符串→§12；B：触碰基线断言）+ 附录 H 撤回记录 | 8 项越权/可疑改动全部撤回并登记（D-4、C-15、C-3、C-11、C-13、C-9 warning 子项、tray 触发路径、tools_tab.py docstring 自纠回退） |

## 2. 本轮已知的结构性限制（如实，不放好看）

1. **只证「文本级零漂移」，不证「数值/时序等价」**：S4 表能证明「某行改了/没改」，不能证明「改得对」。
2. **105 条 `▲ 无护栏`**：§3(48)+§4(14)+§10(19)+其余含 `[对拍]/[M]` 39 条——回归只可能在真机暴露。
3. **37 条非数值无护栏项**（GUI/训练/autodl 驱动）：S4 不覆盖（清单见《S4-数值对照表》§0.2）。
4. **git diff 噪声**：纯移动/重排显示为删+增；本轮逐 hunk 人眼复核已区分，未发现重排型伪变更。

## 3. 实机验收清单（唯一剩余关卡 · 用户执行）

> 以下任一项异常，按「bug-by-bug：根因定位 → 修复 → rebuild → retest」闭环处理。

| # | 场景 | 关注点（对应无护栏条目） |
|---|---|---|
| 1 | **实时变声**（长期运行 ≥10 分钟） | 咬字（音域映射/中值滤波）、音准不飘（F0↔特征对齐，R-1）、延迟量级、CUDA Graph 无爆音卡顿（R-2：graph_key 含 return_length2）、快慢歌声转折 |
| 2 | **离线转换**（长音频） | 任意长不 OOM、首尾无爆音、音质与实时链一致 |
| 3 | **D-3 模型信息查看**（§12.5 验收样例） | 取 info 非空与 info 为空的两个 `.pth` 各点一次「查看」：L1 内容、L2 是否存在；「zip 原名」框自动填入的是归档原名而非 info；「修改 zip 原名」后自动重跑检视显示新名 |
| 4 | **训练 GUI**（短跑 + 中断续训） | 训练启动、loss 正常下降、中断续训、数据加载不报错（NamedTuple collate/pin_memory 路径） |
| 5 | **autodl 云训练向导** | 环境体检、参数向导、步骤执行、断点续训（金标准交互已随 tests 删除，仅存交互实跑） |
| 6 | **启动/托盘** | `--infer`/`--train` 秒开、关窗最小化到托盘、托盘开始/停止、双输出 |

## 4. 结论

- 静态可证的部分**全部通过**：delta 精确 80/50/1；数值冻结零漂移；唯一行为变更 D-3 按授权落地并实测通过；8 项越权全部撤回。
- 等价性的最终判定权在**真机实跑**（§3 清单）——这是本轮验证策略（§13）的既定收口方式，不是缺陷遗漏。
